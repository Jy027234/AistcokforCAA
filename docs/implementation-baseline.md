# 统一实施基线：M/Q 对照任务表

**版本：v0.1（启动基线）**
**编制日期：2026-09-13**
**依据：**《A-Quant-Lab_开发文档_v0.2.md》（主规格）、接入补充 v0.2.1 / v0.2.2、`docs/integration/q0-readiness-report.md`
**状态：** 待评审。本文件是**实施基线**，不替代主规格；冲突时以主规格为准并回到本文件登记修订。

> 目的：把主文档的 M0–M5 与补充文档的 Q0–Q5 合成**一条**可执行主线，消除两份文档在排期与前置条件上的矛盾，并明确每项任务**依赖什么、如何验收**。

---

## 1. 为什么需要这份文件

主文档与补充文档各有一套阶段划分，且存在三处实际矛盾：

| 矛盾 | 主文档 | 补充文档 | 本基线裁决 |
|---|---|---|---|
| 数据源决策的时点 | M0 是**第一道门**，M0 未完成不得进入 M1 | v0.2.2 §1 一处要求 Q1 前关闭数据源，§4 又把供应商决策排到第 5–7 周 | **拆成两件事**：真实数据验证从第 1 周并行启动；供应商**合同**可后置。见 §3 T3 |
| 是否依赖既有 AI 基座 | §0.1"独立轻量部署，不依赖已有企业平台底座"；`agentctl` 零命中 | v0.2.1"优先复用已有 agentctl" | 以主规格 §0.1 自身的例外条款衔接："不在未检查现有代码的情况下设计强耦合集成"。现已检查完毕，故接入成立，但**须新增 ADR 并修订 §14.3/§14.4**。见 §6 |
| Q3 的账本前置 | M2 才交付模拟与账本 | Q3 前置仅写"Q1 + 合成账户"，却要求验证"仅一次入账" | **Q3 依赖 M2**；且必须区分"确认冻结一次"与"模拟成交记账一次"。见 §3 T9 |

结论：**Q 系列依附于 M 系列，不是一个平行的第二条主线。** 接入是纵向能力，量化领域是横向底座。

---

## 2. 主线形状

```text
              M0 数据与许可验证 ──┐
                                   ├─→ M1 数据与证据底座 ─→ M2 基线与模拟账本
  真实小样本验证（第1周并行启动）─┘                              │
                                                                  ├─→ M3 可用工作台
  合成数据骨架（第1周，不依赖供应商）─────────────────────────────┤
                                                                  ├─→ M4 事件研究与对照
  Q0 ✅ 已完成 ─→ Q1 适配层 + 只读能力 ────────────────────────────┤
                                                                  └─→ M5 稳定化
                  Q2 证据研究 ← 依赖 M1 证据域 + Q1
                  Q3 预览/确认 ← **依赖 M2 账本** + Q1
                  Q4 作业与追踪 ← 依赖 M1 任务机制 + Q2
                  Q5 双重验收 ← 依赖 Q1–Q4 + M2/M4
```

---

## 3. 任务表

字段：**编号 | 交付物 | 依赖 | 完成判据 | 对应原文档**

### T1 统一实施基线（本文件）
- **交付物**：本文件；架构决策登记；版本锁定表；依赖矩阵
- **依赖**：无
- **完成判据**：每项任务都能答出"依赖什么、如何验收"；三处矛盾已裁决
- **对应**：v0.2.2 §1 全部卡片的总前置

### T2 版本与基座锁定
- **交付物**：`adapters/agentctl/LOCKED_BASE`（提交 SHA、锁定日期、diff 摘要）；基线监测规程
- **依赖**：无（Q0 已产出事实）
- **完成判据**：`b5cad04836cf93b35750fb0116ab0c2d936d1f89` 记录在案；升级 = 显式任务且须重跑 A01–A16
- **对应**：v0.2.2 §5

### T3 可运行骨架（**最高优先，不依赖供应商与模型密钥**）
- **交付物**：
  - `contracts/{event,snapshot}.schema.json`（主文档 §15.3/15.4）
  - `schema/001_metadata.sql`（主文档 §15.1：元数据 + 账本基础结构）
  - `configs/research.example.yaml`（主文档 §11.2 参数）
  - `examples/` 合成样例（强制 `SYNTHETIC` 水印）
  - `tests/validate_spec.py`（只校验契约/样例/SQL）
  - `src/aquant/` 领域包骨架 + `pyproject.toml`
- **依赖**：T1
- **完成判据**：**无数据、无模型密钥**也能运行确定性测试（主文档 §18.4 明确要求）；契约与 SQL 自检通过
- **对应**：主文档 §0 资料包（**当前全部缺失，见 Q0 报告 §6**）、v0.2.2 §4.3

### T4 真实数据验证（**与 T3 并行，第 1 周启动**）
- **交付物**：数据能力卡；脱敏原始样本；历史覆盖/时点报告；许可清单；真实费用与规则确认表
- **依赖**：T1；账号/凭证（**不依赖 T3 完成**）
- **完成判据**：明确"能支持什么、不能支持什么"；至少一个真实最小闭环数据集可验证；四态区分（计划接入/凭证已配置/测试通过/用于正式研究）
- **对应**：主文档 §6.2 M0 硬交付、§19 M0
- **说明**：这是本基线对第 1 处矛盾的裁决——**验证工作立刻开始，合同采购可后置**。工具接通后才发现核心数据取不到，是主文档 §21 明确列出的风险。

### T5 领域底座（M1）
- **交付物**：证券/状态、交易日历、行情、快照、事件原文归档、任务机制（`PENDING→RUNNING→SUCCEEDED/FAILED/BLOCKED` + 租约 + 幂等键）
- **依赖**：T3、T4（至少一个真实最小数据集）
- **完成判据**：**D01–D08 全过**；不可变快照测试通过；新闻归档与数据接入同步运行
- **对应**：主文档 §19 M1、§18.1

### T6 基线与模拟账本（M2）
- **交付物**：S1 基线、确定性组合构建、冻结计划、A股有限日频模拟器、费用与公司行为、日终净值
- **依赖**：T5
- **完成判据**：**S01–S10 全过**；`Decimal`/整数分记账；日终不变量失败**阻断净值发布**（不是告警）
- **对应**：主文档 §19 M2、§12、§18.2

### T7 工作台（M3）
- **交付物**：四导航、研究卡、自选、草稿确认、差异预览、决策日志
- **依赖**：T6
- **完成判据**：用户无需操作数据库即可完成一次完整流程
- **对应**：主文档 §19 M3、§5

### T8 Q1 适配层与只读能力（**首个能力已完成**）
- **交付物**：`src/aquant/adapters/agentctl/`（client/errors/config/onboard）；`capabilities/` manifest + handler
- **依赖**：T2、T3（合成快照）
- **完成判据**：**已达成**——模型自主选中 `aquant.research_card.read` 并真实执行；tenant/product/scope 三类负向全部被拒
- **对应**：v0.2.1 Q1、v0.2.2 §1 Q1、Q0 报告 §4.5–4.6
- **遗留**：其余七个能力按 T9/T10/T11 分批接入

### T9 预览与确认（Q3，**依赖 M2**）
- **交付物**：`aquant.simulation_plan.preview`、`aquant.portfolio.read`、`aquant.watchlist.add`；产品侧确认入口；幂等去重存储
- **依赖**：**T6（M2 账本）** + T8
- **完成判据**：A07/A08/A09 全过；**且必须分别验证两件事**：
  1. **确认冻结**恰好发生一次（计划版本、快照版本、账户状态版本、确认主体、有效期五项复核）
  2. **模拟成交记账**恰好发生一次（这是 M2 的能力，不是确认动作的副产品）
- **对应**：v0.2.2 §1 Q3
- **修正说明**：v0.2.2 把 Q3 前置写成"Q1 + 合成账户"，但"仅一次入账"是账本属性。**确认 ≠ 成交**；若把二者混为一条断言，会在 M2 尚未交付时产生虚假通过。

### T10 证据研究（Q2）
- **交付物**：抽取 handler、结构化输出验证管线、原始输出归档（含内容哈希）、降级路径
- **依赖**：T5（证据域）+ T8
- **完成判据**：A05/A06/A10/A15；失败路径不产生伪证据；归档哈希可复算
- **对应**：v0.2.1 Q2、主文档 §18.3（≥200 份人工标注材料）

### T11 作业与追踪（Q4）
- **交付物**：`aquant.experiment.submit` / `aquant.job.status`；关联表；七态状态机
- **依赖**：T5（任务机制）+ T10
- **完成判据**：A13/A14；七种状态在 UI 与 API 可区分
- **对应**：v0.2.1 Q4

### T12 双重验收（Q5 / M5）
- **交付物**：`q5-acceptance-report.md`（接入侧）+ 量化领域回归报告（**分别出具，互不替代**）
- **依赖**：T5–T11
- **完成判据**：A01–A16 + D01–D08 + S01–S10 全过；备份恢复/崩溃重启/幂等重试/权限不足/数据源失败五项演练完成
- **对应**：主文档 §18.4、v0.2.1 Q5

---

## 4. 依赖矩阵（速查）

| 任务 | 硬依赖 | 可并行于 |
|---|---|---|
| T1 基线 | — | — |
| T2 版本锁定 | — | T3, T4 |
| T3 骨架 | T1 | T4 |
| T4 真实数据验证 | T1 | T3 |
| T5 M1 底座 | T3, T4 | — |
| T6 M2 账本 | T5 | — |
| T7 M3 工作台 | T6 | T8 |
| T8 Q1 适配层 | T2, T3 | T7 |
| T9 Q3 预览确认 | **T6** + T8 | T10 |
| T10 Q2 证据 | T5 + T8 | T9 |
| T11 Q4 作业 | T5 + T10 | — |
| T12 双重验收 | T5–T11 | — |

**关键路径**：T1 → T3 → T5 → T6 → T12。**Q 系列不在关键路径上**，这是"Q0 未关闭不应阻塞独立的数据、契约与账本开发"的结构化表达。

---

## 5. 验收结论的三级口径（防止再次出现过度断言）

Q0 期间出现过一次"把测试接线错误当成产品缺陷"的误判（见 Q0 报告 §4.4）。为避免复发，本基线要求所有验收结论标注级别：

| 级别 | 含义 | 允许的措辞 |
|---|---|---|
| **L1 现象** | 只记录观察到的输入/输出 | "在 X 配置下观察到 Y" |
| **L2 行为** | 在**明确声明的配置**下，断言可复现 | "在配置 X 下，Y 被拒绝" |
| **L3 推断** | 由 L2 外推到未测配置 | **必须显式标注外推范围与未验证前提** |

规则：
1. 任何"系统缺陷"结论必须是 **L2**，且配置前提写在同一句里。
2. **测试接线本身是安全前提，必须被断言而非假定**——两个探针现在都要求产品令牌与 master 令牌分离。
3. 探针脚本**必须用退出码表达结论**，不能只是打印。收到响应 ≠ 通过。
4. 不修改基座即可解决问题时，不提出修改基座的建议。

---

## 6. 需要新增/修订的架构决策

| 编号 | 决策 | 状态 |
|---|---|---|
| ADR-011 | 复用 agentctl 作为受治理的 AI 运行与助手能力，**不**让其接管量化事实、计算与账本 | **待你批准** |
| ADR-012 | `TextModelProvider` 保留为领域窄接口，agentctl 网关为其**生产实现**之一，离线测试替身不变 | 待批准 |
| ADR-013 | 静态 master 令牌不下发到产品；部署一律 `--token-env`；产品使用独立签发的受限令牌 | 待批准（依据 Q0 §4.4） |

同时需修订主文档：§14.3 领域模块（加 agentctl 适配层）、§14.4 仓库布局（`src/aquant/adapters/agentctl/`）、§16.3 工具名 → `aquant.*` 能力 ID 映射表、§20 增补 ADR。

---

## 7. 待你决策的开放项

1. **资料包缺失**：主文档 §0 的 8 项交付物是"未放入工作区"还是"本未生成"？决定 T3 是"补齐"还是"从零设计"。
2. **生产部署启动方式**：是否使用 `--token-env`、master 令牌由谁持有（Q0 报告 O7）。
3. **数据源路线**：商业终端 / 券商接口 / 开源抓取库的取舍，决定 T4 的取证方式。
4. **Q 系列是否继续依附 M 系列**：本基线按"依附"编写；若你要求接入先行，需调整 T9/T10 的依赖（但 T9 无法脱离 M2）。
