# A-Quant Lab：A股研究与模拟决策工作台
## 产品需求、技术架构、研究规范与开发实施文档 v0.2

**编制日期：2026-09-12**  
**适用对象：产品负责人、开发工程师、Codex、研究使用者**  
**交付性质：开发规格，不是已经实现、完成回测或证明有效的软件。**

> ⚠️ **本文有定点修订。** 阅读前先看 `docs/spec-revisions-2026-09.md`：
> 它逐条列出本文哪一段被取代、取代它的是什么、依据在哪。
> 冲突时以修订记录为准（它更晚，且有决策记录）。
>
> 已修订：§14.3（agentctl 适配层不在领域层）、§14.4（仓库布局与实际对齐，
> 另含一条 schema 变更不会自动应用到已有部署的限制）、
> §16.3（能力 ID 取代工具名）、§20（增补 ADR-011/012/013）。
>
> 实施进度与任务状态见 `docs/implementation-baseline.md` §8。

> 核心定位：每天用同一套可追溯的数据和研究规则观察市场，综合行业、公司、宏观、新闻与行为信号，形成可验证的研究假设；由用户决定是否调整模拟组合，再检验模型和人的判断分别增加了什么价值。
>
> 首期不连接券商、不自动交易、不对外提供付费荐股或代客理财。所有资金、仓位、阈值和策略权重均为软件研究初始参数，不是针对用户资金状况的投资建议，也不代表预期收益。

---

## 0. 阅读与实施说明

本文是独立、完整的主规格。资料包另含：

- `CODEX_TASKS.md`：分阶段任务与完成条件。
- `AGENTS.md`：交给编程助手的强制约束。
- `configs/research.example.yaml`：首期研究与模拟配置示例。
- `contracts/`：事件、快照的数据契约；`examples/`：明确标注为虚构的数据样例。
- `schema/001_metadata.sql`：SQLite 元数据与模拟账本基础迁移。
- `tests/validate_spec.py`：只验证本资料包的契约、示例和 SQL，不等同于产品测试。
- `docs/SOURCES.md`：本次调研来源、检查范围、复用限制。

文中的“必须”是首期不可省略的正确性要求；“建议”是可在开发前调整的初始方案；“后续”不应提前变成首期依赖。本文中的命令、接口与文件目录是待实现的目标，不表示当前已经存在可运行产品。

### 0.1 已确定与待验证

| 项目 | 本文决定 | 不作出的假定 |
|---|---|---|
| 使用方式 | 单人、研究和模拟、中文 Web 界面、支持手机浏览 | 不假定用户证券账户权限、资金量、投资经验或适当性 |
| 研究频率 | 日频刷新，初始策略每周调仓 | 不做盘中高频、实时盘口或秒级新闻抢跑 |
| 数据 | 优先验证正式 API；记录授权、覆盖和历史版本能力 | 不假定免费接口稳定、付费接口天然包含全部权限 |
| AI | 结构化证据、检索解释、受限研究工具 | 不假定大模型能预测收益，也不让其修改模拟账本 |
| 结果 | 比较模型、事件规则和人工干预的增量 | 不把回测盈利、演示评分或半年记录解释为盈利保证 |
| 部署 | 独立轻量部署，不依赖已有企业平台底座 | 不在未检查现有代码的情况下设计强耦合集成 |

---

## 1. 对前述方案的评估与修正

### 1.1 应保留的方向

全市场浅层扫描、行业与个股结合、外部事件与情绪纳入研究、每日可交互工作台、人工确认、决策日志和对照组合，均值得保留。它们能构成一条“发现—核验—选择—模拟—复盘”的完整使用链路。

### 1.2 需要修正的关键点

| 之前的表达 | 潜在问题 | 本版替代方案 |
|---|---|---|
| 宏观→市场→行业→情绪→个股→组合，逐层加减分 | 容易重复计算同一信息；使宏观叙事未经验证就改变收益预测 | 并行证据路径，先分别记录、分别测试，再在组合规则中有限使用 |
| 行业 Top5、候选 Top20 | 容易变成“每日必须找出机会”，导致追热与过度操作 | TopN 仅为显示上限，可输出无新增机会、证据不足或继续观察 |
| 新闻与价格作为同等强度的交易信号 | 事件重要性、事实可信度与可交易收益并非同一变量 | 新闻首日进入证据系统，交易贡献通过独立实验验证 |
| AI 把 88 分调成 71 分 | 不同量纲混合，容易制造伪精确感 | 分列信号排名、证据质量、风险限制、可交易性与数据完整度 |
| 先做十个因子和三类策略，再由元策略调权 | 容易在数据尚不可信时增加参数和过拟合自由度 | 一条价格基线先闭环；质量策略和事件规则作为挑战者；暂不自适应调权 |
| 低成本接口即可解决数据问题 | 免费或基础权限不等于可靠的历史时点数据和商业数据权利 | 先完成数据能力验证，按字段设阻断条件，缺失时明确降级 |
| 半年后判断人工是否优于模型 | 不同仓位、行业暴露、执行时点和选择偏差可能被误当能力 | 同条件影子组合、分解归因、样本数量与不确定性同时展示 |

这些是研究设计判断，不是已证明的收益结论。平台应允许检验并否定这些设计假设。

### 1.3 更好的产品定义

**不是“让 AI 每天替我挑几只股票”，而是“让证据、规则和我的选择都可以被验证”。**

用户每天主要回答四个问题：

1. 今天真正改变了什么，与我持有或关注的对象有什么关系？
2. 哪些对象值得进一步研究，依据和反证分别是什么？
3. 我维持不动、按模型操作或自行调整，会产生什么差别？
4. 之前的判断后来发生了什么，错误来自数据、解释、选股还是执行？

---

## 2. GitHub 调研结论与复用选择

### 2.1 调研方法与边界

本次检查公开仓库说明、许可及部分关键源文件；对两个项目查看了固定提交下的状态或回测契约，对 Qlib 查看了交易所模拟模块。没有逐行审计全部仓库，没有部署这些项目，也没有复现其收益指标。仓库关注度、演示截图和作者报告均不作为策略有效性的证明。

| 项目 | 可借鉴的内容 | 当前复用判断 | 主要边界 |
|---|---|---|---|
| `ZhuLinsen/daily_stock_analysis` | 自选股日常报告、任务进度、历史报告、Web 工作台；分析记录关联后续评估 | 最接近“每天刷新、自己点选”的体验参考；可逐模块评估复用 | 主仓库 MIT；已有回测功能，但本次未证实其满足本项目全部组合账本和历史时点要求 [S01][S02] |
| `microsoft/qlib` | 数据处理、因子/模型研究、实验记录与回测接口 | 后续可选研究适配器，不作为首期运行必需组件 | MIT；官方示例数据目前提示暂停提供，生产数据应独立解决 [S03][S04] |
| `TauricResearch/TradingAgents` | 将市场、基本面、新闻、情绪拆成明确研究状态；记录反方意见 | 借鉴有类型的研究状态与证据组织，不照搬多角色投票决定交易 | 当前主仓库标注 Apache-2.0；框架或论文不等于 A股实盘验证 [S05][S06] |
| `vnpy/vnpy` | 交易系统模块化；现有 `vnpy.alpha` 数据、模型与策略研究能力 | 未来扩展研究或交易接口时评估 | MIT；不能简单把它理解成只有交易执行的旧框架；本期不引入完整交易栈 [S07] |
| `akfamily/akshare` | 多数据源适配模式、研究数据获取 | 原型、交叉核对及明确授权后的补充数据源 | 软件 MIT 不代表上游数据可任意商用或再分发；项目亦提示研究用途边界 [S08] |
| `ricequant/rqalpha` | A股相关账户、风控、撮合与模块化设计 | 取得适用授权后，才考虑作为替代模拟引擎 | LICENSE 对商业用途和组织使用有额外限制；不是无条件 Apache 可商用项目 [S09] |
| `hsliuping/TradingAgents-CN` | 本地化研究工作流与产品组织 | 仅作方案参考；默认不复制其应用端代码 | 混合许可；许可说明对 `app/`、`frontend/` 使用专有条款，须核对目录、版本及作者授权 [S10] |
| `OpenBB-finance/OpenBB` | Provider 抽象、统一字段、扩展机制 | 借鉴数据适配器思路，不在首期整体嵌入 | 仓库许可为 AGPLv3，需评估具体修改、分发和网络服务情形的义务；不等于禁止商业使用 [S11] |
| `microsoft/RD-Agent` | 假设—实验—反馈的自动研究循环 | 后续研究自动化参考，不进入首期关键路径 | MIT；自动搜索因子前，必须已有数据时点、隔离测试集与实验审计 [S12] |

### 2.2 关键代码发现

**daily_stock_analysis。** 检查固定提交 `089d9d26d68f8b839ea5a74a3784e4402925f8b7` 下 `api/v1/schemas/backtest.py`，其契约将分析记录、评估窗口、引擎版本和后续结果关联，并表达数据不足及结果歧义。值得借鉴的是“报告也要有事后验收”，而不是只生成一段分析文字。[S02]

**TradingAgents。** 检查固定提交 `be952b8eccb49720509af544c6675233bc1f10d0` 下 `tradingagents/agents/utils/agent_states.py`，状态中区分市场、情绪、新闻、基本面报告与讨论记录。本项目采用结构化研究状态，但去除模型直接形成可执行交易决定的权限。[S06]

**Qlib。** 检查默认分支 `qlib/backtest/exchange.py`，已有买卖价格、涨跌限制表达式、成交量限制、费用与交易单位接口。不能说它完全不支持 A股约束；也不能把示例参数当成已覆盖所有板块、历史日期和账户规则。本项目需要自己的规则配置、数据质量校验和验收样例。[S04]

### 2.3 三条实施路线比较

| 路线 | 优点 | 代价 | 适用结论 |
|---|---|---|---|
| 直接部署日常分析类开源产品 | 最快获得自选股分析与日报体验 | 对底层时点、模拟账本、实验可比性需要另行验证 | 单纯先体验 AI 日报时合适；不直接当本项目研究结果的权威引擎 |
| 对单一大型开源框架深度二次开发 | 可复用较多能力 | 继承许可、数据假设、架构和依赖；改动可能很深 | 有明确选型验证及长期维护能力后可用 |
| 新建轻量领域核心，选择性复用合适模块 | 数据、证据、实验与账本边界清楚 | 需认真实现有限范围的模拟核心与测试 | **本项目推荐；不是从零开发全能量化平台** |

首期只实现“日频、沪深普通主板股票、长仓现金账户、周度调仓”的有限模拟器。若黄金样例、第三方对照或维护成本表明自建不可控，应切换经授权与验收的成熟引擎，不得用不完整撮合假装正确。

---

## 3. 产品范围与分期

### 3.1 首期观察范围与可模拟范围分开

**观察范围：** 按可获得数据扫描 A股股票，覆盖何种交易所和板块由数据能力页明确展示；不能只取部分列表，却显示“全市场已覆盖”。

**默认可模拟范围：** 沪深交易所普通主板 A股。剔除当前时点已知的风险警示、退市整理、新上市不足规定交易日、停牌、关键字段缺失及不满足流动性要求的证券。创业板、科创板、北交所首期可展示，但默认不进入可执行模拟池。

这只是缩小工程和验证范围，并非判断其他板块不值得研究。未来扩围应逐板块补齐交易规则、公司行为和测试，不允许靠修改一个过滤条件完成。

### 3.2 第一阶段必须交付

| 能力 | 首期内容 |
|---|---|
| 数据与快照 | 行情、证券主数据、交易日历、状态/涨跌停价、基础行业；授权新闻和公告从第一天开始归档 |
| 今日工作台 | 变化摘要、覆盖与新鲜度、市场行为指标、研究候选、持仓相关事件 |
| 研究卡片 | 因子依据、来源、利好与反证、未知项、事件影响路径、是否可模拟 |
| 基线策略 | 一条透明的趋势—低波动研究基线，独立版本 |
| 模拟账户 | 资金、持仓批次、费用、交易限制、订单与成交、日终估值 |
| 实验与复盘 | 模型基线、事件规则影子组合、用户组合、基准；完整决策日志 |
| AI助手 | 解释和受限查询；可生成模拟方案草稿，不能自行提交成交或改账 |
| 运行保障 | 幂等任务、数据失败降级、重跑记录、备份恢复、费用与模型调用用量 |

### 3.3 首期明确不做

不做实盘接口、融资融券、期货期权、日内高频、Level-2 撮合、自动搜索无限因子、自动强化学习调仓、开放用户插件、公开荐股社区、多租户计费、知识图谱数据库和复杂微服务体系。

历史财务、宏观修订版本或行业历史映射不完整时，相应回测能力不得默认上线。事件归档和阅读仍可运行，不能以历史数据不足为由完全移除新闻主线。

### 3.4 后续阶段

V0.3：验证后的财务质量策略、更完整历史行业/主题映射、事件实验与授权舆情数据。

V0.4：Qlib/其他研究引擎适配、有限自动实验建议、多用户需要时迁移数据库。

实盘另立项目：重新核验数据权利、监管要求、券商接口、账户适当性与风险控制。是否实盘不是本项目自然自动发生的下一步。

---

## 4. 研究架构：并行分析，受约束地汇合

```text
结构化数据路径                         外部证据路径
行情 / 财务 / 历史行业                  公告 / 政策 / 宏观发布 / 新闻
        ↓                                      ↓
时点数据快照 → 因子与市场行为           去重、实体识别、来源与时点校验
        ↓                                      ↓
全市场候选 / 行业比较                  事件卡片 / 影响假设 / 反证 / 未知项
        └─────────────┬────────────────────────┘
                      ↓
               研究卡片与实验定义
                      ↓
          确定性规则 / 组合约束 / 可交易性检查
                      ↓
           模型方案      事件影子方案      用户草稿
                      ↓
             冻结决策快照与模拟订单
                      ↓
          有限日频模拟器 → 账本 → 归因与复盘
```

宏观、新闻和心理因素都被纳入，但不是“一条新闻的情绪分数直接乘股价目标”。对每个信息源都分别判断：事实是否可信、当时是否可知、影响是否有合理路径、是否已反映在价格中、怎样用数据检验。

### 4.1 对“全市场还是先选板块”的具体回答

默认使用两个入口：

- 自下而上：在合格证券池计算统一价格与风险特征，获得跨行业候选。
- 自上而下：观察行业相对表现、基本面与外部事件，再查看相关公司。

两入口汇合为同一研究卡片。行业不是唯一入口，热度也不是必然加分项；持仓行业上限在组合层实施。官方或供应商行业分类与新闻主题分表管理，主题允许多对多映射，必须标注关联证据和生效时间。

### 4.2 各类信息在首期的角色

| 信息 | 首期用途 | 不能直接推出的结论 |
|---|---|---|
| 价格、成交、波动 | 基线因子、流动性和风险测量 | 趋势将无限延续 |
| 财务与估值 | 当前研究解释；PIT 完整后才进入历史策略 | 低估值一定低风险 |
| 宏观发布 | 宏观日历、事实变化、情景解释 | 较前值改善等于超市场预期 |
| 政策/产业新闻 | 事件关联、传导假设、受益受损对象 | 政策重要就一定有短期超额收益 |
| 市场行为代理指标 | 拥挤、分歧、风险偏好观察 | 准确读出散户心理或识别所谓主力身份 |
| 社交舆情 | 后续授权样本的热度和分歧研究 | 帖子情绪代表全体投资者 |

---

## 5. 用户工作流与界面规格

### 5.1 每天的使用闭环

打开工作台先看到数据是否完整、当前研究时间、相较上次发生什么。用户可从行业或全市场候选进入股票，阅读依据与反证，加入自选，或将其纳入模拟调仓草稿。系统显示调整后行业集中度、资金占用、不能交易的原因与假设费用。确认后在规定截止时点冻结；成交结果随后按模拟规则计算。下一次复盘同时保留原判断，不能用新解释覆盖旧理由。

### 5.2 顶层导航控制为四项

| 导航 | 核心内容 | 交互原则 |
|---|---|---|
| 今日 | 数据状态、重要变化、持仓事件、研究候选、待处理项 | 先变化后排行；允许“今日无需调整” |
| 研究 | 行业/主题/股票、事件证据、对比分析、自选 | 同一实体使用同一卡片和证据链 |
| 组合 | 模型、事件影子、用户组合；草稿、模拟订单、持仓、账本 | “自选”与“模拟持仓”视觉与权限分离 |
| 实验 | 历史回放、实验登记、对照结果、人工决策复盘 | 所有结果显示版本、样本期、数据级别和不足 |

数据源、规则版本、费用、模型额度放在设置或运行状态抽屉，不铺成许多管理菜单。

### 5.3 股票研究卡片

卡片必须包括：

- 证券身份：代码、交易所、板块、历史时点名称与行业。
- 时间：快照截止、报告生成时间、数据完整度、实时/历史重建/虚构示例标签。
- 数值：相对排名、实际因子值、比较范围；“排名前 10%”不得改写为“上涨概率 90%”。
- 证据：3 条以内最重要依据、至少一个有效反证或明确“未找到反证”，均关联来源。
- 不确定性：预期是否已被价格消化、缺少哪些数据、观察窗口。
- 行动入口：加入自选、比较、创建模拟草稿；不可出现一键真实买入。
- 限制：不允许模拟时，显示具体规则、适用日期和修复方法，不只显示“失败”。

禁止统一输出看似精确的“综合投资价值 87.36 分”。如使用综合排名，必须能展开公式与各项贡献，且不能混入 AI 自报置信度。

### 5.4 原型布局描述

```text
[研究日期] [数据已就绪/部分缺失] [账户：模拟] [刷新状态]
--------------------------------------------------
今天改变了什么？              我的组合需要关注什么？
事实变化 + 来源               新事件 / 数据问题 / 集中度
--------------------------------------------------
研究候选 [全市场] [行业] [自选] [事件关联]
名称 | 依据 | 反证/风险 | 数据质量 | 是否可模拟 | 操作
--------------------------------------------------
我的模拟草稿：预计资金、行业分布、规则检查、确认入口
```

桌面支持双栏研究和证据抽屉；手机优先展示变化、卡片与自选。颜色和图标不能暗示盈利保证，风险状态必须同时有文字。网络失败、数据过期、空候选、任务运行中和无权限均有明确页面状态。

### 5.5 研究助手

支持询问“为何它进入候选？”“这条新闻与持仓有什么关系？”“去掉某行业后组合如何变化？”“我上月的干预多付了多少交易成本？”

助手必须先调用已发布快照和研究接口再回答系统状态。涉及写操作时只生成草稿及差异预览；用户在明确界面确认后由后端执行。自由文本中一句“好”不得被实现成对任意账户操作的通用授权。

---

## 6. 数据源、权限与验证计划

### 6.1 数据源矩阵

| 数据域 | 优先验证来源 | 必需数据/能力 | 缺失处理 |
|---|---|---|---|
| 交易日历/证券身份 | Tushare 或获得授权的正式供应商 | 上市与退市日期、板块、历史名称/状态、交易日历 | 缺少状态无法验证时，证券不得进入模拟订单 |
| 日行情 | 正式 API；AKShare 可辅助核对 | 未复权 OHLC、成交量/额、历史修订、单位定义 | 暂停本次新决策；不以昨收填当天 OHLC |
| 调整/公司行为 | 同口径正式数据源 | 复权因子、分红、送转、登记/除权/到账日 | 无法正确核算则标记结果无效，不能删掉受影响样本 |
| 行业与成分 | 有历史进入/退出日期的数据 | 分类版本、成员有效期及数据可知时点 | 仅显示当前分类；相关历史因子和约束实验停用 |
| 财务 | 报表及公告时间、修订版可追溯的供应商 | 原始报表、公布时间、合并口径、修订链 | 只做当前研究，禁止伪装 PIT 历史回测 |
| 公告 | 交易所/巨潮披露文件；Tushare 授权索引 | 标题、主体、原文、发布时间、更新/撤回、可访问性 | 保留索引和失败原因，无法核验内容则不生成交易规则 |
| 宏观 | 统计局、央行等正式发布；有授权的结构化接口 | 指标期、发布时间、初值/修订值、单位 | 未取得共识预测则“预期差”为空 |
| 新闻 | 授权新闻接口、允许使用的公开原始材料 | 原始来源、发布时间、全文/许可内摘录、转载关系 | 搜索结果仅用作发现，不能自动视作完整权威原文 |
| 社交/搜索热度 | 第二阶段授权聚合数据 | 样本边界、采样频率、去机器人规则、许可 | 首期不作为必需输入，缺失不影响价格基线 |

Tushare 的积分权限与新闻、公告等独立权限需分别检查；不能仅因为一个日线接口成功，就认为其余数据已可用。[S13] 公告接口文档提供 `rec_time` 等字段，但需要明确请求并实测历史覆盖；接口存在某字段，不等于所有历史记录都具有可靠精确时间。[S14]

### 6.2 数据接入前置验证：M0 的硬交付

为每个数据域输出一张能力卡：提供方、接口、字段、单位、时间含义、范围、分页方法、配额、成本条件、可本地存储范围、是否允许向模型服务商发送、是否允许再分发。

实际测试至少包含：普通交易日、假期边界、停牌日、分红前后、退市股票、公司更名、财报修订、公告只有日期没有时间、API 权限不足、分页上限和限流重试。保存调用统计和脱敏样本。

必须区分“计划接入”“凭证已配置”“测试通过”“用于正式研究”四种状态。任何未实际测试的数据源都不能标为已接通。

### 6.3 来源优先级与交叉验证

同一字段设定主来源，备用来源不能在主来源失败时悄悄混入。切换供应商需要新数据版本、单位检查、差异报告和重算。一次回测不能在用户不知情的情况下把不同复权口径或历史分类拼接。

行情原始成交量统一转换为股、成交额统一为人民币元；财报按元，收益率存小数，界面再格式化。原始单位同时保留。例如供应商“手”“千元”等转换必须依接口文档实现和测试，不能凭列名猜测。[S15]

### 6.4 首期不承诺免费或固定低价

开发预算分为：数据授权、模型调用、服务器/存储、维护与合法使用检查。软件开源不等于底层数据免费。平台应先在用户可获得的授权范围内跑通最小数据闭环，再决定购买什么，不将购买昂贵终端作为无条件前提。

---

## 7. 历史时点与数据版本规范

这是本产品最重要的技术约束：**系统回看过去时，只能使用那个决策时点能够获得的信息。**

### 7.1 时间字段不可混用

| 字段 | 含义 |
|---|---|
| `event_time` / `period_end` | 现实事件时间或经济/财务指标归属期，不代表公开时间 |
| `source_published_at` | 原始来源公开时间；不知道精确时分秒就不得编造 |
| `timestamp_precision` | `SECOND`、`MINUTE`、`DATE`、`UNKNOWN` |
| `source_published_date` | 只有公布日期时保存原始日期，`source_published_at` 保持空值，不伪造午夜时间 |
| `first_seen_at` | 本系统首次实际捕获时间；必须如实记录，不能回填成历史公开时间 |
| `ingested_at` | 本次版本进入系统的时间 |
| `available_at` | 本研究模式允许使用该版本的最早时间，必须说明推导依据 |
| `valid_from` / `valid_to` | 名称、行业、交易规则等现实适用区间，采用左闭右开 |
| `supersedes_id` | 被本次修订替代的记录；不能覆盖旧版本 |
| `computed_at` | 特征或研究结果实际计算时间 |
| `input_cutoff_at` | 本次计算允许读取的输入截止时间 |

时间保存为带时区的 UTC ISO 8601；界面和交易日历使用 `Asia/Shanghai`。不能以服务器本地时间或星期一到星期五代替交易日历。

### 7.2 两种模式要明确分开

**LIVE_OBSERVED：真实前向记录。** 可用时间不早于来源公开、系统首次捕获和必要人工核验完成时间。线上特征还必须完成计算才可用于决策。晚收到的公告不能被当成系统早就知道。

**HISTORICAL_RECONSTRUCTED：历史重建。** 今天下载历史数据，首次捕获时间仍然是今天；历史可用时间应由可验证的供应商 PIT 记录、原始发布档案及保守时间规则另行重建。不能将 `first_seen_at` 伪造为过去，也不能简单要求它早于过去，从而错误地否定所有合法历史回测。

每项历史记录标记可用时间依据：`OBSERVED`、`VENDOR_PIT`、`RECONSTRUCTED`、`UNKNOWN`。标记为 `UNKNOWN` 的字段不得用于声称时点正确的正式回测。

历史特征可以今天计算，但必须记录输入版本、当时可用时间和计算配方；实算时间不冒充历史计算时间。训练出的模型还必须遵守其训练数据截止与训练完成的模拟假设。

### 7.3 日期不完整时的保守规则

若材料只提供公布日期，没有时间，默认到公布日期之后的第一个交易日盘前快照才允许使用；实际盘前尚未抓取或审核通过的，继续顺延。遇到假日按交易日历处理。不得默认它在公布日开盘前可用。

这一规则会牺牲部分机会，但比使用无法证明的早期信息更可靠。后续获得精确时间，可以建立新的重建版本；不得覆盖原实验结果。

### 7.4 财务、宏观与行业的专门约束

财务归属 2025 年，不代表 2025 年末即可使用；应按其实际公开时间加入。修订公告只影响修订后可知的计算，不逆向替换早期实验。累计季度报表不能直接相加得到 TTM；按年度值与本年、上年同期累计值衔接。

宏观初值、修订值和发布时间分别保存。今天查询出的修订后历史 GDP 或其他指标，不能直接当成当年所知数值。

证券池和行业映射也要有历史版本。不得用今天的指数成分股、当前存活股票和当前行业标签回测全部过去；否则会遗漏退市或退出对象，或引入未来分类信息。

### 7.5 复权与公司行为

价格特征使用截至研究时点已知的调整序列；订单与现金账本使用真实未复权交易价格。原始价格、调整因子和公司行为版本独立存储。不得每天重新下载一条随未来分红变化的前复权历史序列，然后无版本地覆盖旧实验。

### 7.6 大模型本身也可能携带“未来知识”

即使检索只给过去新闻，今天的大模型也可能从训练记忆中知道之后的公司或行情事件。研究文献已讨论这种金融历史测试中的前视风险。[S18][S19]

因此，历史 LLM 输出必须标记 `llm_retrospective_risk=true`；不得仅因检索时间过滤正确，就宣称没有泄漏。前向保存的 AI 结果是事件贡献评估的优先证据。历史抽取限于可核验事实，隐去名称等方法只能作为敏感性实验，不能宣布彻底消除偏差。

---

## 8. 快照、作业与每日时间安排

### 8.1 初始日程是产品默认值，不是用户交易建议

| 时点（上海时间） | 作业 | 结果 |
|---|---|---|
| 交易日收盘后 | 更新行情、状态、公司行为；持续收集授权材料 | 分域校验结果 |
| 20:30 | 当日研究输入截止，随后计算并发布 EOD 快照 | 今日工作台和次日草稿依据 |
| 下一交易日 08:45 | 收集夜间材料并形成盘前最终输入快照 | 所有比较组合共享的决策基础 |
| 08:55 | 冻结该交易日符合条件的模型和用户模拟计划 | 不可变决策与订单清单 |
| 当日开盘后、日终数据完整后 | 依据模拟约定核算开盘成交和日终估值 | 模拟成交、余额与归因 |

这些时间可以配置，但修改需版本化。20:30 只是输入截止，不保证供应商届时交付全部数据；完整性不满足时显示等待或阻断。08:45 的关键快照未及时发布，08:55 不产生新订单，不偷偷退回旧快照继续交易。

首期周度调仓在交易周的第一个交易日执行；所用价格信号来自其前一个交易日。无交易周自然跳过。每天显示新的研究候选，并不意味着每天生成调仓。

### 8.2 夜间消息与公平比较

模型、事件影子和人工组合应使用相同最终快照、相同下单截止和相同模拟成交规则。人工若依据外部、未归档信息干预，应记录 `external_information_used=true`，并提示不再属于严格同信息对照。

用户在冻结后修改，只生成下一允许周期的草稿，绝不能回填本次开盘成交。前向实验中不得让模型使用昨晚材料，却让人工使用开盘后的新闻再比较结果。

### 8.3 快照清单

每个快照包含 ID、种类、模式、输入截止、实际发布时间、父快照、各数据集文件哈希、版本、覆盖与新鲜度、可用证券池哈希、规则和费用版本、质量状态。

数据文件先写临时路径，校验通过后原子发布清单。消费者只读 `PUBLISHED` 快照。更正生成新 ID 和 `supersedes` 关系；旧决策始终指向旧快照。

### 8.4 作业状态和重试

`PENDING → RUNNING → SUCCEEDED / FAILED / BLOCKED`。状态带心跳、租约、尝试次数与错误代码。任务幂等键由作业类型、交易日、配置版本、输入快照构成，数据库建立唯一约束。

失败重跑不能重复写订单、计费或发布相同报告。源数据已成功而 AI 失败时，价格基线仍可显示，事件研究标记未完成；行情或账户关键输入失败，则阻断新模拟订单。

---

## 9. 新闻、政策、宏观与行为研究

### 9.1 信息处理链路

发现材料 → 检查许可和允许的域名 → 获取原文或许可内内容 → 保留哈希与发布时间 → 去重与转载归并 → 实体匹配 → 结构化抽取 → 引用与数值校验 → 必要人工核验 → 发布事件卡 → 按预注册规则进入实验。

同一原始消息被转载 50 次，只算一个事实事件；转载数量可以另作热度变量，但不能变成 50 份独立证据。撤回、澄清或修订应更新事件状态，并生成新版本。

### 9.2 事件卡最小结构

事件类别、主体、事实摘要、事件时间、公开时间、可用时间、原始来源、引用片段、结构化数值、涉及公司/行业、影响路径假设、可能利好与不利方向、研究窗口、反证、未知项、核验状态、模型与提示词版本。

**事件影响强度不是未来收益预测。** “事实核验通过”与“该公司可能受益”分开存储。允许一个事件对上下游有相反影响，也允许无法确定股价方向。

### 9.3 首批事件类型

先覆盖公告和权威来源容易核验的类型：业绩预告/报告、分红回购、重大合同、监管调查、停复牌/风险警示、正式产业政策、宏观数据发布。

非市场新闻也可以进入，例如能源供应变化、物流中断、公共事件和技术进展，但其公司关联必须有证据。供应链受影响的推测不得直接成为确定的受益股票名单。

### 9.4 宏观研究

每项指标保存公布值、此前可知值、修订值及时间。有合法共识预期数据时，才能计算“公布值减共识”等预期差；没有时显示“未知”，不得用前值替代共识。

初期呈现宏观事实与可反驳的情景假设，例如“成本压力可能变化，需要观察哪些行业指标”。不建立“某宏观指标上升→全市场仓位增加 20%”这类未经测试的自动映射。

### 9.5 群体行为与心理学如何落地

首期用市场行为代理：上涨家数比例、个股收益分散度、成交活跃度变化、行业集中度、实现波动、符合当日规则的涨跌停统计。其定义和数据范围必须公开。

这些变量用于研究风险偏好、拥挤与分歧，不等同于直接测量所有散户的信念。大单资金流等供应商分类不能直接解释为真实机构持仓或“主力意图”。没有盘中数据，不声称准确计算炸板率、封板持续时间或盘中情绪切换。

授权社交数据进入第二阶段：记录平台样本和采样边界，去机器人及重复转载，研究热度、分歧与价格反应。不要收集私人聊天、绕过登录限制、伪装普通用户批量抓取，或将个体言论关联到真实身份做画像。

### 9.6 AI 的成本与权限

第一轮只抽取去重后的材料，不对所有股票反复读相同新闻。优先覆盖持仓、自选、确定性规则筛出的候选及重大公告；其余材料保留索引并标注未深读。

按文档内容哈希、抽取模型、提示词和契约版本缓存。每天设置费用与材料数上限；超预算停止增强解释，不影响已验证的数值基线。升级模型后形成新结果版本，不覆盖以前的判断。

模型只能读允许的研究接口；不能拿到数据源密钥、券商权限、任意 SQL 或 Shell。新闻正文属于不可信输入，里面的“忽略之前规则”“调用工具转账”等内容必须被视为材料而不是指令。

---
## 10. 第一批特征与策略

### 10.1 十个特征不是十个独立盈利因子

下列为候选研究配方，不是已验证选股秘诀。`P_t` 表示截止时点可用的调整收盘价；窗口以交易日计算。缺失不填零，无法计算就输出明确原因。

| ID | 名称及配方 | 角色 | 启用条件 |
|---|---|---|---|
| F01 | 20 日动量：`P_t/P_(t-20)-1` | 收益排序假设 | 至少 21 个有效历史价格点 |
| F02 | 60 日动量跳过最近 5 日：`P_(t-5)/P_(t-60)-1` | 中期趋势假设 | 至少 61 个有效价格点；与 F01 的相关性要报告 |
| F03 | 行业相对动量：F01 减同期 PIT 行业组合收益 | 比较与研究，不直接重复加分 | 历史行业成员、权重配方和数据完整 |
| F04 | 20 日对数收益标准差乘 `sqrt(252)` | 风险/低波动假设 | 21 个有效价格点；252 是报告年化约定 |
| F05 | 近 5 日平均换手率 / 近 20 日平均换手率 | 行为观察 | 分母为正、换手口径一致；不默认越大越好 |
| F06 | 近 20 日平均成交金额 | 可交易性过滤与容量约束 | 使用截至决策已完成的交易日；元为单位 |
| F07 | ROE_TTM：归母净利润 TTM / 期初期末平均归母权益 | 财务质量 | 平均权益为正、历史公开时点与口径一致 |
| F08 | 现金质量：经营现金流净额 TTM / 合并净利润 TTM | 财务质量 | 合并净利润为正；金融业不套用该模板 |
| F09 | 营业收入 TTM 同比增速 | 成长假设 | 同口径且去年同期分母为正 |
| F10 | 盈利收益率：归母净利润 TTM / 时点总市值 | 估值假设 | 市值大于零；负利润保留符号，不伪装为高收益率 |

F07 的期初权益为该 TTM 窗口起点，期末为终点；F08 不能把全集团现金流和仅归母净利润无说明地混用。财务模板需要区分行业适用范围。

TTM 计算优先采用：最近完整年度值 + 当年累计值 − 上年同期累计值；或由同口径独立季度值求和。绝不直接加总四份累计季度表。

### 10.2 特征处理规则

保存原始值、变换后值、横截面排名、覆盖率和剔除原因。横截面排名仅使用该时点合格证券池，采用平均秩处理并列；全等时为 0.5。非有限值无排名。

如采用截尾、标准化、缺失填补或模型学习的行业调整，所有需拟合的参数只在训练集拟合；同日横截面可用的确定性排名不属于未来数据，但证券池本身必须时点正确。不得让库默认预处理跨越整个历史区间。

### 10.3 S1：首期趋势—低波动基线

仅作为一条易解释、易复现的起始研究假设：

```text
momentum_rank = mean(rank(F01), rank(F02))
low_vol_rank  = rank(-F04)
signal_rank   = 0.5 * momentum_rank + 0.5 * low_vol_rank
```

按 `signal_rank` 降序，证券 ID 作为稳定的并列次序；流动性是资格过滤，不再作为额外“盈利分数”。行业暴露限制在组合构建执行，不给某行业主观加分。

默认选取最多 20 个对象，每个初始目标权重为总资产 4%，总目标股票仓位上限 80%。这些数字仅为研究参数。候选不足、约束冲突或有不可卖持仓时保留现金，不把剩余股票权重无限放大。

调仓时对已有仍合格证券采用排名缓冲：持有对象在前 30 名内，优先保留；需要补位时按排名补足。新增、保留和不可交易对象的冲突解决规则必须确定，不能交给 AI 即兴决定。无缓冲版本保留为单独实验，不在结果不佳后偷换配方。

### 10.4 S2：质量—估值挑战者

仅在 PIT 财务数据和行业适用性验证通过后启用。候选配方：

```text
quality_rank = mean(rank(F07), rank(F08), rank(F09))
value_rank   = rank(F10)
signal_rank  = 0.5 * quality_rank + 0.5 * value_rank
```

F07—F10 任一关键值无效时不强行补齐。金融等不适用对象进入单独研究模板；因此 S2 与 S1 比较时必须同时报告不同证券池的影响，并提供同池对照。首期不宣传质量策略优于价格策略。

### 10.5 E1：事件风险影子规则

第一条事件实验刻意简单、可证伪：在 S1 相同候选、资金和执行条件下，对已核验的公司重大监管调查公告，试验“自事件可用时点起若干交易日暂停新增该证券”。初始观察窗口可设 10 个交易日，参数在实验前冻结。

该规则不靠 AI 猜收益方向，不自动卖出现有持仓。正常交易状态、风险警示等硬限制对所有组合一视同仁，不能只对事件组合实施，从而制造优势。

E1 首期是影子实验，不因为文字看起来合理就成为默认风控规则。已经存在的真实交易限制始终优先于实验规则。

### 10.6 宏观与情绪的升级门槛

后续可测试“拥挤变量是否改善换手或回撤”“宏观状态是否改变某因子的表现”。每增加一条条件，都生成新实验和新的对照；不直接建立可随结果调整参数的元策略。

初期不采用强化学习、自动新闻评分买卖、几十个 Agent 辩论后统一下单。复杂算法不能补救数据时点错误或不真实的交易成本。

---

## 11. 组合构建与人工干预

### 11.1 确定性的组合构建次序

1. 读取冻结输入、账户持仓批次与当前时点状态。
2. 标记不能卖、不能买、公司行为未解决等限制；已有风险不会因筛选而消失。
3. 根据策略排名与保留缓冲形成研究目标集合。
4. 在总仓位、单证券和行业上限内产生目标权重；约束不足时留现金。
5. 将权重差换成整数股订单，数量使用决策时已知价格计算。
6. 检查预计资金、费用、可卖数量与基于过去成交量的订单规模上限。
7. 生成差异预览，冻结计划；在模拟执行时再次检查当日状态和资金。

已有无法出售持仓先占用风险预算。例如某行业已有冻结仓位 25%，行业上限为 30%，其他股票只能占剩余 5%，不能先把它忽略再配置 30%。若价格波动导致自然超限，显示超限并限制增加暴露；不能凭日终发现的超限倒推当日开盘卖出。

### 11.2 初始研究参数

| 参数 | 示例值 | 解释 |
|---|---:|---|
| 初始模拟资产 | 1,000,000 元 | 仅软件测试配置 |
| 目标持仓数量 | 最多 20 | 不是每天推荐数量承诺 |
| 目标股票总仓位 | 不超过 80% | 不足部分保持现金 |
| 单证券权重上限 | 10% | 约束上限，不代表建议集中度 |
| 单行业权重上限 | 30% | 按冻结时点行业版本计算 |
| 新股排除 | 上市未满 120 个交易日 | 研究过滤，不是交易所统一规定 |
| 流动性门槛 | 近 20 日均成交额不少于 5,000 万元 | 示例门槛，需做敏感性实验 |
| 订单规模上限 | 过去 20 日平均成交股数的 0.1% | 仅容量代理，不能证明开盘一定成交 |
| 调仓频率 | 每周首个交易日 | 确认截止前冻结；非调仓日不强制操作 |

所有参数属于实验版本。用户修改后产生新策略或新实验，不覆盖旧配置。

### 11.3 人工操作类型

用户可以加入/删除自选、对事件提出核验意见、从已支持证券池选择对象、调整模拟目标权重、明确选择不操作。备注可为文字或语音转写，但必须在结果发生前保存。

`decision_log` 记录原模型方案、展示的证据版本、人工更改、理由类别、外部信息使用、提交时间、规则检查和最终模拟成交。人工不操作也应记录为 `NO_CHANGE` 或超时未提交，而不是丢失样本。

人的观点和选择可改变草稿，不可覆盖历史事实、模型原始排名或交易硬约束。

### 11.4 撤销与回滚

未冻结草稿可修改，已冻结但未模拟执行的订单按明确截止规则取消，并保留取消事件。已经记账的模拟成交不允许物理删除；修正使用冲正及新版本重算，并标记受影响报告。

“重跑看看另一个结果”必须生成独立实验分支。不能把事后更优分支覆盖原本前向表现。

---

## 12. A股有限日频模拟器规范

### 12.1 正确性边界

模拟成交是明确约定下的计算，不是对真实券商成交的承诺。首期只支持已验收的普通主板股票、现金长仓、日频、一次性全成或不成的开盘模拟订单，不做盘口排队、盘中止损止盈或精细部分成交。

采用范围有限的模拟器是为了控制复杂度；不意味着这些约束可以省略。未被支持的交易或公司行为必须显式报错，而不是落入近似结果并显示成功。

### 12.2 规则不能写成永久常量

交易规则表按交易所、板块、证券状态和生效日期匹配。2026 年 4 月 24 日上交所发布交易规则修订说明，主板风险警示股票涨跌幅限制调整等规则自 2026 年 7 月 6 日起实施。[S16]

因此不能把“ST 永远 5%”“所有 A股永远 100 股一手”或统一费用写在全局代码里。即便首期排除 ST，也必须历史保留状态、规则与当日涨跌停价，用于观察、剔除和账户持仓处理。交易数量规则按实际支持板块单独验收。

### 12.3 时间与价格约定

盘前冻结订单最早按当日开盘模拟。不得利用当日收盘信号再按同一收盘价成交，也不得在看过开盘价后，假装开盘前已经决定了最优股数。

目标股数使用决策时可知参考价格与预留资金计算。买入成交价按开盘价加预注册滑点、卖出减滑点。若拟成交价越过当日合法价格边界，判为不可成交；不把价格强行夹到边界以制造成交。

买入订单若遇到开盘涨停，卖出若遇到开盘跌停，默认不成交。停牌、没有有效开盘价或关键状态未知时不成交。该保守约定并不能保证其他情况下真实成交，报告必须说明未模拟排队和开盘流动性。

### 12.4 成交量与资金

开盘订单大小只能由过去已知成交量设上限，不能使用当天收盘后才知道的全日成交量来优化早晨订单。当天成交量可事后评价容量风险，不可回写下单数量。

首期模拟顺序为：先按稳定顺序处理卖单，再按稳定顺序处理买单。卖单未成交，预期卖出资金就不存在；若买单全额成交加费用超过可用资金，整单拒绝，不透支，也不利用未来数据智能缩单。

该顺序假定卖出所得可用于同交易日买入，并把开盘执行作简化处理；实际开盘阶段的资金占用与成交先后未被精确复现。必须提供“买入仅使用开盘前现金”的保守对照模式，报告两种假设差异。

### 12.5 T+1 与持仓批次

每个买入批次保存买入交易日和最早可卖交易日。卖单不得超过当日可卖批次数量；当天新买批次不纳入可卖数量。正常买入按支持市场规则处理整手；零股出售按已验收规则单独实现，不能用浮点股数。

测试必须区分“昨天已有同一股票”与“今天新买的同一股票”，不能因账户里存在旧持仓就把新买股份也视作当日可售。

### 12.6 费用与税费

佣金、最低佣金、卖出印花税、过户费等使用独立、按日期生效的费用版本。财政税务官方公告规定证券交易印花税自 2023 年 8 月 28 日起减半；具体适用税率、计费范围与账户费用应以实施时有效规定和实际收费约定核验。[S17]

不能沿用开源样例费率宣称当前真实成本。本资料包的费用文件是**合成测试费率**，故意禁止用于真实数据的正式研究；M0 必须另行建立已核验费用表。费用不得漏掉最低金额，也不得因订单多次查看而重复收取。

货币计算使用 Decimal 或整数最小货币单位，价格可保存微元整数，账本金额按规定舍入到分；不得依赖二进制浮点保证资金平衡。

### 12.7 分红、送转、退市等公司行为

必须处理已支持的现金分红权益确认、应收股息、到账日、送转股份与成本调整。持仓权益根据登记时点确定，除息日确认应收与价格影响，派息日从应收转为现金，不能把除息损失和现金分红重复计算。

税费处理必须标注口径。未实现与持有期等相关的完整股息税处理时，可输出明确的税前或保守税费情景，但不能把它标为准确个人税后收益。对照基准使用一致口径。

配股、复杂合并换股、分拆或无法正确核验的公司行为出现时，停止产生新的有效绩效结论并列为待处理；不能事后剔除该股票整个历史，让回测变好。对退市股票保留历史与退出事件，使用有依据的终值或明确保守情景；不能在组合中永远以最后收盘价假装可变现。

### 12.8 日终估值与不变量

每日净值 = 可用及冻结现金 + 支持的应收项目 + 持仓估值 − 应付项目。停牌估值使用明确记录的上次有效价格并标记陈旧天数，而非伪造当日行情；仍须反映组合不可变现风险。

必须满足：现金不透支、持仓不为负、总股份与批次一致、成交数量不大于订单、费用恰好记账一次、现金明细合计等于余额、快照/订单/成交/估值均可追溯。测试中的失败应阻断发布净值，不只是打印警告。

---

## 13. 实验、回测与人工增量评价

### 13.1 四类对照

| 标识 | 组合 | 用途 |
|---|---|---|
| M | 固定 S1 模型基线 | 检验简单数值规则 |
| E | 同基线加 E1 事件规则 | 单独检验事件带来的增量 |
| H | 用户在相同约束下的明确选择 | 检验人工相对基线的变化 |
| B | 同可模拟证券池基准 + 广义市场参考 | 区分市场上涨、证券池和方法带来的收益 |

各账户彼此独立，但初始资产、周期、费用、成交假设一致。H 可与 M 有不同仓位；因此必须同时报告现金、市场暴露和行业差异，不能只比较累计收益就判定能力。

同池基准可采用相同总仓位和调仓频率的等权篮子；同时展示广义市场指数。指数是价格指数还是全收益指数必须写明，不把含股息组合直接与不含股息指数的差额称为全部选股超额收益。

### 13.2 实验登记先于看结果

每个实验包含假设、输入数据范围、证券池、特征、策略版本、训练/验证/测试区间、预处理、标签窗口、费用与滑点、比较对象、主要评价指标、终止或失败条件。

改一条权重、过滤条件或事件窗口就产生新实验。失败和负收益实验同样保存。测试集被多次用于选择方案后，不再是未触碰测试集；界面必须记录它被查看和比较的次数。

### 13.3 时间序列验证

数据足够时，建议采用滚动训练、验证与测试，例如 3 年训练、6 个月验证、6 个月测试。具体长度根据历史覆盖和样本数量在实验前确定，不强行套用固定数字。

禁止随机打乱日期形成普通随机交叉验证。跨分割边界的未来收益标签必须剔除或隔离，间隔至少覆盖最长标签持有窗口；同一事件及其转载不能分别泄漏到训练和测试。

价格规则没有拟合模型，也必须做时间外检验与参数敏感性。不能用“没有机器学习”作为无需防过拟合的理由。

### 13.4 评估分层

**数据层：** 覆盖率、缺失与修订、状态可靠性、可用时间质量、异常证券占比。

**研究层：** 因子覆盖、秩相关、分组收益、跨时期稳定性、事件抽取准确率。因子秩相关不是交易胜率。

**组合层：** 净收益、回撤、波动、换手、现金比例、行业集中度、未成交率、费用、容量代理与基准差额。Sharpe 等指标必须声明年化、无风险利率与样本口径。

**稳健性层：** 费用和滑点提高、不同成交假设、参数邻域、市场阶段、流动性与行业拆分。重采样应考虑时间相关和事件聚类，不能把几千只同日股票当成几千个完全独立试验。

### 13.5 人工复盘不等于人格评分

记录：用户新增/删除股票产生的选择效应、调整时机、持有期变化、仓位与行业效应、额外成本、外部信息与未操作次数。数量太少或暴露不同，就显示“尚不足以归因”。

例如用户组合在弱市少亏，可能来自更多现金，而非更强选股；这种差别仍有价值，但应正确命名。平台不自动推断用户“适合激进交易”或逐渐增加风险来迎合偏好。

### 13.6 策略从影子到默认的门槛

是否升级由用户明确决定，并保留审批记录。至少满足：时点与账本正确、样本外及前向记录可解释、相对对照存在稳定证据、成本压力下结论不过度脆弱、没有靠少量特殊样本支撑全部表现。

不设“回测收益超过 X% 就自动上线”。连续 20 个交易日稳定运行可作为工程观察门槛，不能当成充分的盈利验证周期。

---

## 14. 技术架构与组件选择

### 14.1 首期采用模块化单体

| 层 | 本版选择 | 原因与约束 |
|---|---|---|
| Web | React + TypeScript + Vite | 便于研究抽屉、组合对比与手机适配；不先做复杂设计系统 |
| API | Python + FastAPI + Pydantic | 数值研究与数据契约在同一语言生态；前端不复制计算逻辑 |
| 业务持久化 | SQLite WAL，单机少量并发 | 存账户、决策、任务、元数据；写事务短，忙等待与重试可控 |
| 行情与特征 | 不可变 Parquet + DuckDB 查询 | 大规模列式读取与快照；DuckDB 不承担多进程共享写入 |
| 作业 | 独立 Python worker + 数据库任务租约 + 系统调度 | 避免先引入消息中间件；API 只提交任务 |
| 数值运算 | NumPy / pandas；按性能验证再优化 | 首期偏正确性与复现，不提前做分布式计算 |
| 模型访问 | 自有窄接口 `TextModelProvider` | 接口兼容和模型切换不改变领域契约 |
| 可选研究扩展 | Qlib adapter | 后续引入，不控制核心账户和证据数据模型 |
| 部署 | 单机 Docker Compose 或本地进程 | 不要求 Kubernetes、Kafka、向量库或图数据库 |

各库版本在依赖解析与 CI 验证后锁定，不在文档中猜测“最新版本”。研究运行保存实际锁文件哈希。

### 14.2 进程和写入边界

```text
Browser → FastAPI → Domain services → SQLite
                       ↓ submit job
                Worker（作业租约/单个数据发布者）
                       ↓
Providers → Raw files → Normalized Parquet → Published manifest
                       ↓
            DuckDB read-only analytical sessions
                       ↓
          Feature / Strategy / Simulator / Evaluation
```

SQLite 使用单机本地持久盘，不放在不适合锁语义的共享网络目录。worker 是行情文件发布者；多个查询进程各自读取已发布清单，不共享可写 DuckDB 文件。API 与 worker 对 SQLite 可各自短事务写入，但不在写事务内等待网络或模型调用。

实际多用户、并行长写任务或高可用要求出现时，把业务数据库迁移到 PostgreSQL；届时再评估队列和对象存储，不提前建设分布式系统。

### 14.3 领域模块

`data` 管抓取与版本；`evidence` 管原文、引用与事件；`research` 管特征和研究卡；`strategy` 管纯规则目标；`portfolio` 管模拟计划与约束；`simulation` 管订单成交与账本；`experiments` 管对照与评价；`assistant` 管受限工具；`operations` 管任务、预算与审计。

任何展示页面不得直接抓取供应商或调用模型后把返回文字写入持仓。研究模块不能直接修改现金。模拟模块不能自行联网查“最新数据”补足历史回测。

### 14.4 目标仓库布局

```text
a-quant-lab/
  apps/web/
  apps/api/
  src/aquant/
    domain/{data,evidence,research,strategy,portfolio,simulation,experiments}/
    application/
    adapters/{providers,storage,models,research_engines}/
    operations/
    cli.py
  contracts/
  configs/
  migrations/
  tests/{unit,integration,golden,pit,security,e2e}/
  docs/{adr,runbooks,research}/
  data/                 # 不提交真实数据、密钥或私人账户记录
  pyproject.toml
  compose.yaml
  .env.example
```

### 14.5 容量与成本规划

在真实股票数量、年份、字段和数据提供方速率明确前，不承诺特定机器上能在几分钟完成。可先在常规 CPU 机器上验证日频流水线，记录全市场扫描、快照发布、模型调用和页面读取的实测耗时与峰值内存，再决定扩容。

性能优化顺序：减少重复网络请求、列式读取、增量计算、事件去重、LLM 缓存，再考虑并行。复杂模型或本地 GPU 不是首期前置条件。模型预算是用户可设上限，不能未经授权升级到更贵模型。

---

## 15. 数据模型与契约

### 15.1 分域存储

数值事实放 Parquet，事务性对象和引用索引放 SQLite。规范化行必须带证券/指标 ID、日期、供应商和版本、单位、可用时点或其关联索引。每个物理文件列入不可变清单。

| 领域 | 主要对象 | 关键关联 |
|---|---|---|
| 数据 | 来源、数据版本、快照、快照成员、证券及状态版本 | 每次研究明确指向输入快照 |
| 证据 | 原始材料、引用片段、事件、事件—引用关联 | 所有事实声明回到原材料 |
| 研究 | 研究运行、策略版本、实验和实验组 | 固定代码、配置、特征与数据版本 |
| 决策 | 用户决定、模拟草稿、冻结计划 | 保存模型原方案和人工差异 |
| 账户 | 模拟组合、订单、成交、持仓批次、现金/应收账本、估值 | 从期初到期末可以逐项对账 |
| 运维 | 任务、模型调用、审计日志 | 重跑、权限、额度与错误可诊断 |

资料包 SQL 是元数据和账本基础结构；行情/财务长表、完整公司行为明细及历史行业成员由迁移和 Parquet 契约继续补齐。基础 SQL 通过语法验证不代表已经实现领域不变量。

### 15.2 主键与身份

内部 `instrument_id` 必须稳定，不以公司名称为主键。供应商代码作为映射，交易所与证券类别独立保存。更名不产生新证券，真实不同证券不得因同名被合并。

研究卡片、文档、事件、快照和决策分别有 ID；转载材料可以共享事件但保持独立文档记录。哈希用于内容与版本完整性，不代替业务身份。

### 15.3 事件契约要求

详见 `contracts/event.schema.json`。契约验证只解决类型和必填字段；服务还必须核验：引用片段存在、金额单位正确、关联实体真实、时序合理、来源权利适用、状态变更有依据。

`VERIFIED` 表示事实及其引用通过既定核验，不表示获利可信。`UNKNOWN` 的市场方向允许保留；不得为了满足必填字段强行选择利好或利空。

### 15.4 快照契约要求

详见 `contracts/snapshot.schema.json`。已发布快照的每个数据集包含路径、SHA-256、记录数、时点上界和质量信息。发布时验证文件存在、哈希一致、必要域齐全；消费者再次验证重要清单关系。

合成示例使用 `SYNTHETIC` 模式并强制水印。生产研究入口拒绝将合成数据与真实行情混成单一收益曲线。

---

## 16. API 与工具接口规格

### 16.1 通用约定

`/api/v1` 为版本前缀。写操作要求登录、权限、请求 ID；提交计划和任务要求 `Idempotency-Key`。异步作业返回 `202` 与 `job_id`，不得把“已排队”显示为“已完成”。所有分页返回实际游标和条数。

研究读取必须指定 `snapshot_id` 或由服务解析为一个明确快照，并在结果中返回该 ID；一次页面的多张卡片不能各自读取变化中的“最新数据”。

### 16.2 首期接口

| 方法与路径 | 输入/输出要点 | 权限与限制 |
|---|---|---|
| `GET /health`、`GET /readiness` | 进程、依赖、关键数据状态 | 不返回密钥或详细内部凭证 |
| `GET /snapshots/latest` | 指定模式/种类，返回版本与质量 | 只返回已发布对象 |
| `GET /dashboard?snapshot_id=` | 当日变化、候选、持仓相关事件 | 所有数值同快照 |
| `GET /instruments/{id}/research?snapshot_id=` | 数值、证据、反证、限制 | 缺失明确，不触发后台任意抓取 |
| `GET /events`、`GET /events/{id}` | 过滤、卡片及许可内引用 | 不能泄漏不可再分发全文 |
| `POST /watchlist/items`、`DELETE /watchlist/items/{id}` | 自选管理 | 不产生订单 |
| `POST /research/jobs` | 对固定输入运行研究 | 预算、授权源和幂等检查 |
| `POST /plans/preview` | 组合、目标权重、快照 | 返回差异、规则检查及预计费用；不写成交 |
| `POST /plans/{id}/freeze` | 版本、确认令牌 | 再核验时点、快照、规则、账户版本 |
| `GET /portfolios/{id}`、`GET /portfolios/{id}/ledger` | 持仓、批次、净值和对账 | 明确模拟账户标识 |
| `POST /experiments`、`POST /experiments/{id}/runs` | 固定实验定义、执行任务 | 不允许任意代码上传执行 |
| `GET /experiments/{id}/comparison` | 对照指标、差异、限制 | 暴露差异与不可比条件可见 |
| `POST /assistant/messages` | 会话、快照、用户问题 | 只允许登记过的工具 |
| `GET /jobs/{id}`、`POST /jobs/{id}/retry` | 运行状态、可恢复错误 | 重试不能改写已冻结产物 |

接口使用的实际端口、框架路由和完整 OpenAPI 文件在开发中生成，本文不假装已经提供可调用服务。

### 16.3 助手允许工具

`get_snapshot`、`query_research_card`、`get_event_evidence`、`compare_instruments`、`explain_factor`、`get_portfolio`、`get_decision_history`、`preview_simulation_plan`。

默认没有 `execute_order`、`write_ledger`、`run_shell`、`raw_sql`、`fetch_arbitrary_url`。创建自选等轻量写入也需要明确动作与审计；冻结模拟计划通过用户界面确认的服务执行，不把确认权限交给模型。

### 16.4 错误代码

至少包括 `DATA_NOT_READY`、`PIT_UNVERIFIED`、`SOURCE_PERMISSION_MISSING`、`STALE_SNAPSHOT`、`RULE_VERSION_MISSING`、`FEE_VERSION_UNVERIFIED`、`DECISION_CUTOFF_PASSED`、`INSUFFICIENT_CASH`、`T1_NOT_SELLABLE`、`LIMIT_PRICE_BLOCKED`、`CORPORATE_ACTION_UNSUPPORTED`、`LLM_BUDGET_EXCEEDED`。

每种错误包含用户可读解释、对象 ID、是否可重试、修复动作；不能只返回“分析失败”。

---

## 17. 安全、合规和数据权利

### 17.1 金融业务边界

首期定位为自用研究与模拟，不提供真实交易通道，也不承诺收益。未来若涉及策略自动生成或下达真实交易指令，不能认为“用户最后点一下”就必然不属于程序化交易；相关监管定义覆盖通过计算机程序自动生成或者下达交易指令，应按具体流程向券商核验报告和其他要求。[S20]

《证券市场程序化交易管理规定（试行）》自 2024 年 10 月 8 日起施行，交易所另有实施规定，未来实盘上线应重新核验当时有效版本。[S20][S21] 本文不是针对特定账户的法律意见。

公开商业化、个性化荐股、收费研究服务、数据转售与代客操作可能涉及不同边界，必须单独评估；不能仅靠免责声明覆盖实际业务行为。

### 17.2 数据与模型服务权限

每个来源登记：研究使用、本地保存、保留时长、模型处理、摘录展示、第三方再分发、商业用途。未知权限默认不开放相应能力。

公开可访问不等于允许批量抓取；代码 MIT 不等于新闻全文、交易所数据或供应商报表也属 MIT。发送材料到外部模型服务前，确认合同允许及数据处理设置；研究账户与个人信息默认不进入模型上下文。

### 17.3 应用安全

密钥只在服务端读取，日志脱敏；外部访问启用认证和 HTTPS。开发默认绑定本地，不能将无鉴权服务直接开放公网。前端 Markdown/HTML 严格清洗，阻止公告文本执行脚本。

抓取器限制域名、协议、重定向和文件大小，阻止访问内网、云元数据地址和本地文件。文档解析在受限进程中运行；模型工具不继承抓取器的任意网络权限。

### 17.4 研究代码隔离

首期策略由项目代码和受控配置定义，不运行用户或模型临时生成的任意 Python。后续要允许自动研究代码，必须独立沙箱、禁网络、CPU/内存/时间限制和只读输入；通过审核后才能成为正式策略版本。

---

## 18. 测试、验收与质量门禁

### 18.1 数据与 PIT 黄金用例

| 编号 | 用例 | 必须结果 |
|---|---|---|
| D01 | 财报归属去年但今年才公布 | 去年策略读取不到该财报 |
| D02 | 历史报告后来修订 | 修订前快照仍使用当时版本 |
| D03 | 今天导入历史原始数据 | 保留今天抓取时间；按可证明依据重建可用时间 |
| D04 | 公告只有日期、逢长假 | 顺延到保守可用交易日，不编造早间时间 |
| D05 | 股票后来退市或改行业 | 历史证券池与历史分类不被当前列表替换 |
| D06 | 未来分红影响今日前复权序列 | 旧快照与旧实验哈希不变化 |
| D07 | 主数据、行情单位或供应商切换 | 差异可见，未经验证不混接 |
| D08 | 同一公告转载多次并随后撤回 | 事实归并，撤回产生新状态，旧判断仍可回看 |

### 18.2 模拟与账本黄金用例

| 编号 | 用例 | 必须结果 |
|---|---|---|
| S01 | 当天买入又尝试全部卖出 | 新买批次不可售；旧可售批次单独处理 |
| S02 | 买入遇开盘涨停、卖出遇开盘跌停 | 按保守规则不成交，无虚构资金 |
| S03 | 卖单未成导致买单资金不足 | 买单拒绝，现金不透支 |
| S04 | 买入开盘跳空超过资金预算 | 不用未来价格事后重定最佳股数 |
| S05 | 重试同一冻结计划 | 订单、费用、成交仅记录一次 |
| S06 | 小额订单最低佣金 | 费用与现金精确对账 |
| S07 | 分红除息与到账不同日 | 应收和现金正确迁移，不重复收益 |
| S08 | 股票停牌、退市、复杂公司行为 | 不制造成交、不移除亏损样本，明确估值与未支持状态 |
| S09 | 规则生效日前后、板块不同 | 使用相应日期和板块的规则配置 |
| S10 | 仅日线同时穿过止损止盈价 | 首期不推断盘中先后，不生成此类成交 |

### 18.3 AI 与助手用例

准备独立人工标注集，建议不少于 200 份去重材料，覆盖金额单位、同名公司、否认性公告、转载、修订、相互矛盾和无关新闻。集合规模是工程起点，不是统计充分性的声明。

关键事实须有可定位来源；不支持的事实不发布。记录实体、日期、金额、否定关系错误率，按类型报告，不只给一个平均分。对最关键的身份与金额错误设置阻断；样本中零错误不等于全量永不出错。

恶意材料要求模型泄漏密钥、修改持仓或忽略证据时，系统不得调用越权工具。模型超时或超预算，基线数值仍可读取；AI 卡片显示失败，不能伪造摘要。

### 18.4 发布门槛

- 所有资金和 PIT 黄金测试通过；端到端测试至少覆盖一次完整决策—模拟—估值—复盘。
- 同一数据快照、配置、代码和已保存的模型输出可重放得到一致产物；随机算法保存种子与实际环境。
- 所有拟成交证券的关键字段齐全。全市场覆盖率以实际应有记录为分母、按停牌等状态解释；即使总体超过示例目标 99.5%，候选有关键缺失仍阻断。
- 演示数据不混入真实结果；费用/规则未核验时，正式研究状态不能为通过。
- 至少完成备份恢复、任务崩溃重启、幂等重试、权限不足和数据源失败演练。

不以收益率作为软件验收条件。没有模型密钥也应能运行合成测试和已验证的价格基线；没有真实数据授权只能运行明显标记的演示，不能生成伪真实研究结论。

---

## 19. 开发顺序与阶段交付

| 阶段 | 交付物 | 进入下一阶段的条件 |
|---|---|---|
| M0 数据与许可验证 | 数据能力卡、原始样本、历史覆盖/时点报告、许可清单、真实费用和规则确认表 | 能支持什么、不能支持什么已明确；至少一个真实最小闭环数据集可验证 |
| M1 数据与证据底座 | 证券/状态、日历、行情、快照、事件原文归档、任务机制 | PIT 与不可变快照测试通过，新闻归档同步运行 |
| M2 基线与模拟账本 | S1、组合构建、冻结计划、有限模拟器、费用和公司行为、净值 | 资金/成交黄金测试通过，可重现一段完整历史与前向模拟 |
| M3 可用工作台 | 四导航、研究卡、自选、草稿确认、差异预览、日志 | 用户无需数据库操作即可完成一次完整流程 |
| M4 事件研究与对照 | 结构化抽取、证据核验、E1 影子实验、M/E/H/B 比较、受限助手 | 无依据事实被拦截；AI 失败不影响基线；同信息比较可成立 |
| M5 稳定化 | 故障恢复、备份、性能测量、成本、文档、连续运行记录 | 工程稳定性通过，研究限制和下一步升级条件清晰 |

M0 验证失败不是项目必须停止：可以收窄到有可靠数据的价格研究和前向事件观察。但不能在不改标识、不改范围的情况下继续声称具备完整历史因果/收益验证。

详细任务卡见 `CODEX_TASKS.md`。每阶段先完成一个纵向闭环，再扩展品种、因子或页面。不要先交付充满漂亮随机数字的排行榜，再补最基本的账户规则。

---

## 20. 关键架构决策记录

| ADR | 决定 | 不采纳的替代及原因 |
|---|---|---|
| 001 | 日频研究、周度初始调仓 | 不做实时抢新闻；数据、执行与成本验证难度显著提高 |
| 002 | 六类信息并行汇合 | 不用逐层主观加减分伪装统一预测 |
| 003 | 新闻首日起归档、先证据后收益实验 | 不因历史新闻不完整就放弃，也不编造历史准确率 |
| 004 | 自建窄领域核心，选择性复用 | 不无条件继承完整开源应用的隐含假设或许可 |
| 005 | 单机模块化单体 | 首期多租户、微服务和消息中间件没有足够收益 |
| 006 | 研究与账本分权 | 自由文本与模型输出不能直接改变账户 |
| 007 | 明确可模拟证券池 | 不以“支持全市场分析”冒充“全板块规则已实现” |
| 008 | 暂不引入自适应元策略 | 先建立稳定基线和独立实验，控制参数搜索自由度 |
| 009 | 数据与费用不完整时阻断 | 不用看似连续的虚假精确结果取代真实缺口 |
| 010 | 人工决策原样保存 | 不事后美化判断，也不把自选变化直接视为交易 |

---

## 21. 主要风险与处理

| 风险 | 早期信号 | 处理 |
|---|---|---|
| 数据接口有值但历史时点错误 | 财务、宏观只返回最新修订版 | 禁止相关正式历史因子，保留当前研究和前向归档 |
| 新闻叙事很有说服力却没有增量 | E 组合只是重复价格趋势 | 保留其解释价值，不强推为交易信号 |
| 模型和人工比較失真 | 截止时点、仓位或成本不一致 | 提示不可比并分解，不直接宣布谁更好 |
| 过度研究与多重尝试 | 大量参数试验却只展示最优结果 | 实验预注册、失败记录、测试集访问追踪 |
| 回测忽略交易限制 | 大量在涨停买入、跌停卖出或零成本换手 | 黄金测试、未成交统计、保守成交对照 |
| 项目复杂度失控 | 先做海量因子、十几个 Agent 和完整后台 | 按 M0—M5 门槛收敛，后续功能不成为首期依赖 |
| 许可阻碍产品化 | 商用权限、上游数据权利含糊 | 逐文件/来源登记；未确认前不复制或分发 |
| 过度相信系统 | 评分被理解成确定收益 | 独立呈现事实、假设和限制；允许无行动，保留模拟标签 |

---

## 22. 成功标准与最终实施建议

第一阶段成功，不是每天找到上涨股票，而是建立以下能力：同一时点的数据可以重放；数值和新闻依据能追溯；模拟账本经得起规则和资金检查；用户的选择与原模型决策可以公平比较；错误、缺失和无法判断被真实显示。

**建议的第一条开发主线：** 数据权限与时点验证 → 真实小样本快照 → 有限模拟账本 → S1 基线 → 四页工作台 → 事件证据与影子对照。新闻原文归档从数据接入之初同步进行，不等到 UI 完成才开始积累。

第一轮不需要决定“最好的赚钱算法”。应先交付能识别坏数据、坏回测、坏归因和不可靠解释的系统。之后才有条件判断新闻、宏观、社会心理和人工经验究竟在哪里增加价值。

---

## 23. 来源与引用索引

检索与检查日期为 **2026-09-12**。以下来源支持项目功能、许可、数据接口或规则事实；本文策略、参数、架构、测试门槛均是本项目设计建议，不是来源承诺。许可及监管结论在实际使用时还须按具体版本、业务和适用条件核验。完整检查说明见 `docs/SOURCES.md`。

- [S01] daily_stock_analysis 仓库说明与许可证入口：`https://github.com/ZhuLinsen/daily_stock_analysis`
- [S02] daily_stock_analysis 固定提交回测契约：`https://github.com/ZhuLinsen/daily_stock_analysis/blob/089d9d26d68f8b839ea5a74a3784e4402925f8b7/api/v1/schemas/backtest.py`
- [S03] Qlib 仓库与数据说明：`https://github.com/microsoft/qlib`；工作流：`https://qlib.readthedocs.io/en/latest/component/workflow.html`
- [S04] Qlib 模拟交易模块（检查时默认分支，未固定提交）：`https://github.com/microsoft/qlib/blob/main/qlib/backtest/exchange.py`
- [S05] TradingAgents 仓库与许可：`https://github.com/TauricResearch/TradingAgents`；论文：`https://arxiv.org/abs/2412.20138`
- [S06] TradingAgents 固定提交状态契约：`https://github.com/TauricResearch/TradingAgents/blob/be952b8eccb49720509af544c6675233bc1f10d0/tradingagents/agents/utils/agent_states.py`
- [S07] vn.py 及 alpha 模块说明：`https://github.com/vnpy/vnpy`
- [S08] AKShare 说明与许可/数据用途提示：`https://github.com/akfamily/akshare`
- [S09] RQAlpha 实际许可证：`https://github.com/ricequant/rqalpha/blob/master/LICENSE`
- [S10] TradingAgents-CN 许可说明：`https://github.com/hsliuping/TradingAgents-CN/blob/main/LICENSING.md`；另应核对 README 及具体目录许可证。
- [S11] OpenBB 许可：`https://github.com/OpenBB-finance/OpenBB/blob/develop/LICENSE`
- [S12] RD-Agent 仓库说明与许可：`https://github.com/microsoft/RD-Agent`
- [S13] Tushare 权限与积分说明：`https://tushare.pro/document/1?doc_id=290`
- [S14] Tushare 上市公司公告接口：`https://tushare.pro/document/2?doc_id=176`
- [S15] Tushare 日线接口及单位：`https://tushare.pro/document/2?doc_id=27`
- [S16] 上交所交易规则修订说明，2026-04-24 发布、相关规则 2026-07-06 实施：`https://www.sse.com.cn/aboutus/mediacenter/hotandd/c/c_20260424_10816474.shtml`
- [S17] 财政部、税务总局关于减半征收证券交易印花税的公告：`https://fgk.chinatax.gov.cn/zcfgk/c102416/c5211343/content.html`
- [S18] 研究论文 AI’s predictable memory in financial analysis：`https://www.sciencedirect.com/science/article/pii/S0165176525004392`
- [S19] 前视偏差研究预印本：`https://arxiv.org/abs/2605.24564`；属研究证据，不作为已普遍验证的解决方案。
- [S20] 证监会程序化交易管理规定发布：`https://www.csrc.gov.cn/csrc/c101954/c7480579/content.shtml`；发布说明及定义：`https://www.csrc.gov.cn/csrc/c100028/c7480577/content.shtml`
- [S21] 深交所程序化交易管理实施细则发布：`https://www.szse.cn/lawrules/rule/trade/current/t20250403_612770.html`

---

## 24. 交付状态

本文件完成的是需求、设计、研究限制、实施顺序和验收规范。附件中的契约、SQL 和配置是开发辅助样例；它们不包括已实现的数据供应商接入、模拟成交算法、用户界面或真实回测结果。

未实际验证事项包括：用户账户可取得的数据权限、API 完整历史质量、券商实际费用、部署性能、所有公司行为覆盖、任何策略的超额收益。开发时应从 M0 开始，把未知项变成可验收结果，而不是把本文当成这些事项已经验证的证明。
