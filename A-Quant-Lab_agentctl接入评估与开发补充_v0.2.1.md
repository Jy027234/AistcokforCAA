# A-Quant Lab × agentctl：接入评估与开发补充

**版本：v0.2.1（接入补充，不是产品已实现版本）**  
**编制日期：2026-09-13**  
**适用主文档：《A-Quant Lab 开发文档 v0.2》**  
**核验源码：`Jy027234/Agent`，`main` 在本次读取时为 `7e94058f96a4de4b27552a3c46c4529043e1d7e1`。**

> 结论：可以基于现有 agentctl 开发。推荐形态为“agentctl 承担受治理的 AI 运行与助手能力，A-Quant Lab 保留独立量化领域服务和存储”。这不是把整个量化系统改写成一个 Agent，也不是把所有代码合并进基座仓库。
>
> 本次进行了文档与关键源码静态核对，没有登录用户部署服务器、运行 A-Quant Lab、执行联调或重跑 agentctl 测试。仓库中的既有验收结果是作者保存的证据，不是本次重新测得的结果。接入可行不等于已经接入成功。

## 1. 相对 v0.2 的变更范围

v0.2 的第 0.1 节采用独立轻量部署，并明确不在未检查基座代码时设计强耦合集成。本次检查后，建议将生产目标调整为：

**优先复用已有 agentctl；保持量化领域核心独立、可测试、可在 AI 不可用时降级运行。**

“领域独立”与“重复建设 AI 基础设施”是两件不同的事。前者保留，后者避免。

| 原设计部分 | 接入版调整 | 保持不变 |
|---|---|---|
| 模型适配 | 生产优先接 agentctl 模型网关；保留离线测试替身 | 结构化契约、预算、敏感数据检查 |
| 助手编排 | 优先使用现有 `assist` SDK 与原生持续交互 | 产品页面、自选、研究卡与人工决策流程 |
| 工具声明 | 产品自有能力通过 v1 manifest 描述和接入 | 产品服务仍负责领域权限与输入检查 |
| 运维审计 | 关联 agentctl run/trace 与产品 job/research/decision | 产品领域审计、快照及资金账本仍独立 |
| 定时研究 | 首期由产品作业入口控制每日批次；AI 子任务交给基座 | 数据就绪门禁、交易日历、幂等与重跑规则 |
| 部署 | 共享现有 agentctl，另运行轻量 API/worker | 不强迫引入新微服务群、消息中间件或全量企业平台 |

首期仍不接券商、不自动真实交易，不让模型直接冻结模拟计划、制造成交或改动现金。

## 2. 当前源码已经提供的接入基础

### 2.1 首选 SDK，不从旧的大客户端任意挑接口

当前首选 SDK 文档规定，新产品使用 `agentctl.embed.connect`，公共动作词汇为 `connect / apply / invoke / status / close`。SDK 按 `gateway / assist / growth / admin` 提供不同方法面。[C2]

`src/agentctl/embed/__init__.py` 明确产品通过 HTTP/MCP 与能力层通信，tenant 由服务端映射，每次调用携带 request ID；不是要求产品导入 Runtime 内部对象。[C3]

**本项目决策：** 用户交互首选 `assist`。受限批量文本抽取可使用已验证、当前部署允许的类型化网关调用。是否能共用同一部署 profile，需要用实际服务配置验证，不因 SDK 支持某模式就假定服务端已开放。

### 2.2 产品能力 manifest 和确定的处理程序绑定

`capability_apply.py` 将 manifest 投影成产品、操作和能力绑定，并设置 `execution_mode="runtime_capability.invoke"`。代码保存输入/输出 schema、scopes、风险、数据敏感度、确认策略、幂等、回滚、异步生命周期和 handler 信息，还包含冲突分类与移除能力的退役逻辑。[C4]

这说明 A-Quant Lab 的研究查询、模拟预览等可以作为产品自有能力接入，而不必都由模型生成文字模拟完成。

**边界：** 这些字段被保存，不代表金融业务不变量已经实现，也不代表任何自定义 handler 自动安全。manifest 幂等投影也不等同于业务写入恰好执行一次。

### 2.3 生命周期处理程序

`frontdesk/capability_sdk.py` 中的 `FrontdeskCapabilityContribution` 支持 `execute_handler`、`dry_run_handler` 和 `rollback_handler`；执行和回滚是否支持取决于是否提供对应 handler。[C5]

**本项目决策：** 新产品优先按当前 v1 manifest 路线接入；该代码用于确认基座已有生命周期机制，不据此另外建立第二套运行中热注册方案。

### 2.4 产品接入模型与接入检查

`product_integration/models.py` 已有产品记录、能力绑定、操作定义、来源信任与执行产物对象；绑定可以关联 Agent、Skill、Workflow、模型策略和工具权限计划；产物可以关联 run、trace 与内容引用。[C6]

`server/handlers/product_adapter.py` 中存在 registry、onboarding、readiness、certification、admission 等处理入口。[C7]

**边界：** 适配器目录、能力绑定和实际可执行 handler 是不同层面的事实。新产品应使用首选 SDK/manifest 进入既有事实存储，不应另造一套与其冲突的注册表。管理视图显示已注册，也不能替代真实能力调用验收。

### 2.5 近期联调证据的适用范围

`AGENTCTL_NATIVE_DELIVERY_2026-09-11.md` 记录了原生交付、CAplatform 与 Core 的本地联合演练及真实模型合成场景调用；同时明确存在语义质量待改进项、云端尚未切换、生产及公开发行未由该次交付完成。[C8]

**判断：** 已有产品可借鉴的接入路径，不需要为了 A-Quant Lab 先换运行引擎；但不能把其他产品的本地演练直接视为本项目通过验收。

## 3. 推荐架构与职责

```text
A-Quant Lab Web
  ├─ 研究、组合、实验页面
  └─ 助手入口
          │
          ▼
A-Quant Lab API / 后端网关
  ├─ 领域读取、业务鉴权、快照解析
  ├─ 用户确认 → 模拟计划冻结（仅产品受控接口）
  ├─ 领域任务提交 → A-Quant Worker
  └─ Agentctl 适配层 → 现有 agentctl
                           ├─ 模型调用与 AI 研究
                           ├─ 原生助手及产品能力调用
                           ├─ 运行状态、审批上下文、追踪
                           └─ 通过受控 handler 回调产品能力

A-Quant Worker
  ├─ 数据接入、版本化与快照发布
  ├─ 因子、候选与规则计算
  ├─ 模拟成交、估值、账本与对照实验
  └─ 向 agentctl 提交受限研究输入，接收并验证研究产物

独立产品存储：SQLite / Parquet / DuckDB 只读分析会话
现有基座存储：沿用其配置，不与产品直接共表
```

图示是目标部署关系，不是当前已经运行的系统。

### 3.1 应复用基座的部分

复用模型调用入口、受治理的助手循环、产品能力描述与调用机制、运行关联、通用追踪及已启用的授权/审批能力。Agent 的任务可以包括：材料抽取、影响假设、反证整理、研究问答与复盘解释。

一个面向用户的研究助手加少量受控研究能力即可起步。不要把“宏观、行业、新闻、情绪、个股、组合”机械地变成六个长期自治 Agent。

### 3.2 应由产品继续持有的部分

行情与财务的历史时点版本、交易日历、因子公式、证券池、回测、模拟撮合、公司行为处理、现金和持仓账本、实验比较、决策日志，均由 A-Quant 领域代码负责。

模型可以请求执行一个已注册计算，但计算输入、公式、结果验证和账本提交由确定性代码完成。

### 3.3 Platform Core 的使用原则

沿用已部署并验证的统一身份、产品授权和服务间签名上下文；不复制另一个账号、租户或授权真相源。当前 SDK 文档明确 Core 接入需要有效签名上下文，模型输出的 scopes/confirmation 不构成授权。[C2]

本次未完整检查 Platform Core 仓库，也未验证用户服务器的身份链路。开发前需核验实际部署版本及身份契约，不能直接宣布已有环境满足所有要求。个人试用不应为了接入而开启无关的企业管理模块。

## 4. 第一批产品能力

以下为拟新增的 A-Quant 能力名称，不是 agentctl 已内置能力；实际 manifest 字段、枚举和 HTTP handler 配置必须从锁定版本 schema 生成并校验。

| 拟新增能力 | 输出 | 副作用与限制 |
|---|---|---|
| `aquant.market_snapshot.read` | 固定快照的市场摘要、覆盖与新鲜度 | 只读；不得在读取中临时抓取供应商 |
| `aquant.research_card.read` | 个股研究卡、因子依据和限制 | 只读；强制证券 ID 与快照 |
| `aquant.event_evidence.read` | 来源、引用片段、影响假设和反证 | 只读；验证权限、时点与材料授权 |
| `aquant.portfolio.read` | 模拟持仓与风险摘要 | 只读；不能查询其他用户组合 |
| `aquant.simulation_plan.preview` | 计划草稿或无持久化预览、规则检查、预计费用 | 不冻结、不记成交；保存草稿时必须声明真实写副作用 |
| `aquant.experiment.submit` | 有版本的实验 job ID | 有副作用；受控配置、额度、幂等和任务权限 |
| `aquant.job.status` | 运行状态、领域结果与产物引用 | 只读；不把排队显示为完成 |
| `aquant.watchlist.add` | 自选项及写入结果 | 有副作用；仅用户明确意图，幂等和审计 |

所有读取都要验证主体验证后的产品/用户/租户范围。模型提出的 tenant、permission_scope 或对象所有者不能覆盖后端身份。

**不向助手开放：** `execute_order`、`write_ledger`、任意 SQL、任意 Shell、任意 URL 抓取，以及直接冻结模拟计划的权限。自选写入不产生交易。

冻结模拟计划仍由产品界面确认与受控接口完成。基座审批可以成为额外条件，但不能取代对计划版本、快照、账户状态、确认主体和有效期的再次校验。

## 5. SDK 与部署实施约束

### 5.1 默认路线

1. 锁定 agentctl 提交或已验证构建包，核验服务端 profile 与能力。
2. 在 A-Quant API 后端引入 `agentctl.embed.connect` 对应的窄客户端，初始交互选择 `assist`。
3. 在产品仓库维护一个 `agentctl.product_capabilities.v1` manifest，将受控 handler 接入既有运行机制。
4. 在部署阶段通过 `apply` 或当前 integration CLI 投影；不是在每个浏览器请求里运行注册。
5. 在产品后端统一封装 `invoke / status / close`，产品领域模块不依赖 Runtime 内部类型。

当前文档说明 `apply` 不会热插入运行中进程，返回 `runtime_activation=startup_or_reload_required`；注册后需要启动或重载并验证能力真的可用。[C2]

### 5.2 持续对话与状态

Assist/Growth 的当前首选调用默认是 `continuous_assist`，服务端要求 Coordinator v2 和相应权限；Core 路线另需有效签名上下文。SDK 升级不会自动启用服务端功能，拒绝时不能偷偷退回另一种交互流程。[C2]

产品保存自己的消息记录与必要上下文。相同 conversation ID 不保证续接同一个 Run；真正的待答问题按当前 SDK 的控制契约回答，普通新消息是新请求。[C2]

`application_events_v1` 可用于当前响应的卡片展示，但当前文档明确 turn projection 不等于权威对话日志，不能据此承诺完整断线续传。断线恢复按已验证的状态查询与持久记录实现。[C2]

### 5.3 轻量部署

共享现有 agentctl 服务，A-Quant API 与 worker 可先在同一主机上分进程或分容器运行。不要为本项目默认再部署一套基座，不在 agentctl 运行环境里安装整套量化研究依赖，不共用可写业务数据库。

API 调用须有超时、并发和响应体上限。当前 SDK 中使用同步 HTTP 路径的调用，不应直接阻塞异步请求处理线程；通过同步处理器、受限线程池或作业 worker 调用，并进行负载验证。[C3]

已有鉴权或 Core 不可用时，受影响的远程授权操作失败关闭。AI 故障不等于可以绕过身份验证；仍具备有效授权的本地数值查看和确定性研究可以按策略降级继续。

## 6. 每日研究流程

首期由一个明确的产品作业入口负责批次控制，避免产品定时器和基座调度各自执行同一份日常任务。

```text
交易日历与数据就绪检查
  → 抓取 / 校验 / 原始材料归档
  → 发布不可变快照
  → 因子与规则计算
  → 选出需深入研究的持仓、自选和候选
  → agentctl 执行受控材料抽取与研究
  → 产品验证输出结构、引用、实体与时点
  → 保存研究产物并发布工作台
  → 用户观察或创建模拟计划草稿
  → 用户明确确认后，产品冻结计划
  → 领域 worker 模拟、对账、估值和对照评价
```

日频计算不等于每天交易；没有新证据可以输出维持观察。任务具体运行时点应由交易日历与供应商数据发布时间确定，不能单凭“收盘”认定所有数据已经齐全。

后续需要将多步研究迁入基座 Workflow/RunSpec 时，再核验当前部署支持、长作业状态与取消恢复合同，并迁移调度所有权。不能仅因仓库有 workflow 字段就认定首期选用 profile 支持完整编排。

## 7. 研究正确性：基座不能替代的五条边界

### 7.1 Trace 不等于时点回测正确

Trace 可以关联某次调用，不能自动证明输入只包含当时可获得的信息。每次研究至少关联以下产品事实：

`research_run_id`、`snapshot_id`、`as_of_time`、材料/行情版本、策略版本、代码版本、模型与提示版本、已保存模型输出的内容哈希。

接入层再关联基座 `run_id / trace_id` 及现有产物引用，不假定基座顶层协议已有所有量化专用字段。量化字段先保存在产品契约及合法 metadata/引用里。

### 7.2 Memory 与知识检索必须区分当前研究和历史实验

当前研究可使用当前已授权材料；历史实验只能检索相应时点可用的材料和版本，禁止共享“最新事件记忆”直接参与历史选股。

即使检索结果已作时间过滤，也不能据此证明当前模型不含后续事件的训练记忆。因此，用当前大模型重新解释旧新闻应标注为回溯研究；需要严格论证 AI 增量时，优先保存真实前向输出并做持续对照。不把这一限制用一句“禁止未来函数”掩盖。

### 7.3 基座 Ledger 不等于模拟证券账本

agentctl 的运行与用量记录、审计或其他通用 Ledger，不替代 A-Quant 的现金、成交、持仓批次、公司行为及估值账本。

只有 simulation 域可提交模拟成交及会计分录。研究报告写入成功、Run 显示 completed，都不代表订单成交。

### 7.4 回放和回滚不等于改写研究历史

回放使用被冻结的数据、配置与已保存模型输出；重新向模型发同样问题是一次新运行，不保证得到相同结果。

通用 rollback 用于有明确补偿语义的动作。已发布快照、已冻结决定与账本不原地擦除；需要修正时使用新版本、更正记录或明确的冲正机制，不把历史曲线悄悄改好。

### 7.5 新闻是材料，不是指令

公告、新闻及社区文本不获得工具权限。数据源许可、模型外发许可、引用可定位性由产品验证；模型声称 VERIFIED 也不自动成为领域事实。

批量行情不进入长提示或通用向量库作为计算真相源。模型只接收必要的结构化数值和受许可材料；查询量化数据由产品工具在固定快照上完成。

## 8. 双向接入的安全与幂等

产品调用 agentctl：服务凭证只在后端，tenant/user 从受信任身份映射，绑定 source product 与请求上下文，按实际有效授权限制 scopes。

agentctl 回调产品：使用当前已验证的 handler 认证合同；验证调用身份、作用域、对象归属、租户、允许操作以及过期/重放约束，不能因为来自内网就不鉴权。文档不虚构具体签名头，实施时按锁定代码的合同补齐。

对每个可能产生写入或收费的操作：

- 产品使用业务幂等键与持久去重记录；相同 request ID/trace ID 本身不是充分去重保证。
- 同键同请求返回同一 job 或结果；同键不同请求明确冲突。
- 回调重复、超时重试、乱序完成均不能重复入账或覆盖新版本。
- 取消是请求，不是“资金状态已恢复”的证明；按领域实际状态返回。

长作业提交返回 job ID 与当前状态，不用一个 HTTP 请求等待整个回测。产品 job 是领域任务事实；agentctl Run 是编排事实，二者一对一或一对多显式关联，不互相伪造完成。

## 9. 给 Codex 的实施任务

### Q0：锁定部署与可接入性

核验实际服务版本、profile、认证和 Core 上下文，验证一个网关/助手调用，核验 manifest 加载方式。使用无私人持仓的合成数据。输出可复核的接入报告。

完成条件：真实后端身份可通过、无权请求被拒绝、返回状态与服务端一致；若部署落后于核验提交，先明确升级范围，不直接假定兼容。

### Q1：适配层和一条只读能力

新增 `adapters/agentctl/`、一个受限 research-card handler、manifest 与配置。通过后台服务与运行时的双向请求读取一个固定合成快照。

完成条件：同快照读取一致；篡改 tenant、instrument 或权限被拒绝；注册后重载生效；真实能力结果进入助手回答，不以预写文字冒充工具执行。

### Q2：AI 证据研究

接入抽取任务、输入来源校验、结构化输出验证、引用与时点核验、原始输出归档。支持超时、额度、拒绝和降级。

完成条件：恶意材料不能触发写入；失败不发布伪证据；AI 停机时既有数值基线可在有效授权下读取。

### Q3：模拟预览与产品确认

加入组合读取、模拟预览、必要的自选写入。保留现有产品确认入口，不给模型冻结或写账权限。

完成条件：预览不产生成交；旧确认、错误主体、变更后的账户版本和重复提交均正确处理；合法确认后仅一次入账。

### Q4：作业与追踪联动

接入研究/实验任务提交及状态查询，统一关联产品 job、research run、decision 与基座 run/trace。明确调度所有权。

完成条件：队列、运行、等待确认、阻断、失败、取消、完成可区分；重复回调和断线重试不产生额外副作用。

### Q5：运行和研究双重验收

执行下面的用例，同时继续执行 v0.2 的时点、模拟成交、费用、公司行为和资金对账测试。接入验收不能替代量化领域验收。

## 10. 必须补充的接入验收

| 编号 | 用例 | 必须结果 |
|---|---|---|
| A01 | SDK 模式与服务端 profile 不匹配 | 明确拒绝，不静默切换 |
| A02 | 能力 apply 后未激活运行配置 | 不宣称工具已可用；完成重载再做 smoke |
| A03 | 浏览器伪造 tenant 或扩大权限 | 服务端映射与授权拒绝 |
| A04 | 一条研究工具真实调用 | 返回可核对的 snapshot、数据与执行关联 |
| A05 | 新闻要求忽略权限、改仓或泄密 | 无相应工具权限，拒绝越权 |
| A06 | 模型自称 completed/approved | 不改变真实任务或确认状态 |
| A07 | 同一实验提交重复十次 | 返回同一作业或明确冲突，不启动十份计算 |
| A08 | 预览模拟计划 | 不产生订单成交或现金变化 |
| A09 | 确认之后账户/快照已变 | 拒绝陈旧提交，要求重新核验 |
| A10 | 模型或基座不可用 | AI 部分显示真实失败；有效授权的确定性能力按策略降级 |
| A11 | 当前事件混入旧时点实验 | 被时点门禁拦截，原实验不被污染 |
| A12 | 相同固定输入与保存的模型输出回放 | 量化产物一致；新模型运行单独登记 |
| A13 | 任务取消、重复回调、乱序回调 | 领域状态不倒退，已提交账本不被误回滚 |
| A14 | 用户离开再打开助手 | 从持久消息/任务事实恢复，不靠单次 UI 投影伪造历史 |
| A15 | 供应商和模型原始密钥扫描 | 前端、日志、manifest 和产物中无明文密钥 |
| A16 | 跨进程故障后恢复 | 数据快照与账本一致，任务可重试且不会重复入账 |

## 11. 不建议本期做的事情

不将所有业务实现搬进 `Agent` 仓库；不把整个产品压成一个 Skill；不为每一类信息创建自治 Agent；不重造 Frontdesk；不默认切换运行引擎；不将示例 `echo` 或 fixture 通过解释为真实模型与真实金融数据验收通过。

不为使用基座而直接上多租户销售、实盘交易、复杂市场预测、多策略自学习、开放代码执行或新数据库集群。

## 12. 对原开发文档的落地修改索引

| 原章节 | 应补充或替换的内容 |
|---|---|
| 0.1 部署 | 改为优先复用 agentctl，领域核心独立；保留离线测试和故障降级 |
| 3.2 AI助手及运行保障 | 标明复用现有原生助手和产品能力机制，领域账本继续独立 |
| 14 技术架构 | 加入 agentctl 适配层、回调边界、独立进程和部署 profile 核验 |
| 15 数据模型 | 新增产品对象与基座 run/trace 的关联，不复制基座内部表 |
| 16 API/工具 | 加入 v1 manifest 映射和 handler；模型工具仍无冻结/入账权限 |
| 17 安全 | 加入双向认证、后端 tenant 映射、历史 Memory 隔离和外发限制 |
| 18 测试 | 添加 A01–A16，保留原 PIT 和模拟账本全部黄金用例 |
| 19 开发阶段 | 在 M1 前完成 Q0；M1–M4 中分别落地 Q1–Q4，M5 完成 Q5 |
| 20 ADR | 新增“复用运行基座但不让其接管量化事实与账本”的决策 |

本文件为增补设计，未覆盖或改写 v0.2 原文件，也未修改 GitHub 仓库。

## 13. 核验来源与范围

所有代码来源固定在 `Jy027234/Agent` 的提交 `7e94058f96a4de4b27552a3c46c4529043e1d7e1`。下列路径是本次实际读取的主要证据；大文件仅依据返回的可见关键部分形成有限判断，不宣称逐行审计全仓库。

- **[C1]** GitHub `branches/main`：确认本次读取时默认分支提交和提交日期（2026-09-11）。
- **[C2]** `docs/AGENTCTL_PREFERRED_SDK.md`：首选 SDK、模式、持续交互、manifest 激活、类型化网关及 Beta 边界。
- **[C3]** `src/agentctl/embed/__init__.py`：SDK 边界、服务端 tenant、请求关联与 HTTP 实现。
- **[C4]** `src/agentctl/product_integration/capability_apply.py`：产品/操作/绑定投影、元数据、冲突与幂等处理。
- **[C5]** `src/agentctl/frontdesk/capability_sdk.py`：产品自有能力及 execute/dry-run/rollback handler。
- **[C6]** `src/agentctl/product_integration/models.py`：产品、能力绑定、操作、来源信任及执行产物数据模型。
- **[C7]** `src/agentctl/server/handlers/product_adapter.py`：接入目录、就绪、认证、准入等处理入口。
- **[C8]** `docs/AGENTCTL_NATIVE_DELIVERY_2026-09-11.md`：本地原生交付、联合演练、模型语义限制及部署未关闭事项。
- **[D1]** 本对话的 `A-Quant-Lab_开发文档_v0.2.md`：本项目已约定的范围、数据、权限、账本和实施顺序。

固定源码入口：

`https://github.com/Jy027234/Agent/tree/7e94058f96a4de4b27552a3c46c4529043e1d7e1`

**最终建议：从第一版就按现有 agentctl 的产品接入机制建设，但把有限量化核心做成可独立验证的领域服务。基座负责 AI 如何被授权和运行，产品负责数据、计算、实验与账本是否真实正确。**
