# A-Quant Lab API 身份接线示例。
#
# 这是本机单用户演示边界，不是生产认证：
# - API 仍接收 X-Aquant-Subject 标签，不接受 Bearer 伪令牌；
# - LOCAL_LOOPBACK_DEMO 依赖部署端口只绑定宿主机 loopback；
# - AQUANT_TRIAL_SUBJECT 只是固定的主体标签，不是共享秘密。
AQUANT_IDENTITY_MODE=LOCAL_LOOPBACK_DEMO
AQUANT_TRIAL_SUBJECT=user:demo
